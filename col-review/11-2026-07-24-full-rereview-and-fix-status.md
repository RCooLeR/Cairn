# 2026-07-24 Full Re-review and Fix Status

- Re-review date: 2026-07-24
- Re-review base: `a223bfb462037969d42277ef20dcae28468e5f8e`
- Branch: `codex/review-remediation`
- Scope: complete current working tree, including uncommitted remediation changes
- Relationship to the original review: append-only delta to the 2026-07-15 `BE-*`, `FE-*`, and `OPS-*` catalog

## 2026-07-28 superseding acceptance note

The source and package-acceptance status in this report is preserved as historical evidence. Use `12-2026-07-28-registry-credential-helper-and-final-acceptance.md` for the current registry/helper, single-instance, and cross-platform build/CI review, `RR-2026-031` through `RR-2026-041`, final-tree evidence, and exact Windows binary metadata.

RR-2026-026 is no longer residual: Agent status/tool-catalog refresh now has monotonic request-generation ownership across success, error, and `finally`, with reordered-response coverage. RR-2026-029 is closed for the exact hashed standalone Windows executable after passing PE/build inspection and native WebView/provider/project/registry smoke. Native relaunch exposed RR-2026-039, where a second full Cairn runtime contended for the first process's private named pipe; Wails single-instance ownership now precedes provider/bridge startup, and native relaunch leaves one process/window with projects intact. OPS-014 is also closed for the final tree: a real clean `npm ci` generated the nested `frontend/node_modules/go.mod`, and root `go list ./...` returned 24 packages with zero beneath `frontend/node_modules`. Final release audit then exposed RR-2026-040 and RR-2026-041: native Linux/macOS builds still allowed build-time module mutation and stale artwork, while hosted/local vet omitted repository tooling. Native package paths now enforce `-mod=readonly` without tidy, shared icon generation synchronizes canonical assets first, and CI/local lint vet `./...`. These updates do not close the bundle-budget, architectural, supply-chain, installer, signing/publication, platform-native package, external Docker-config-writer, or other residual findings retained below.

## Executive status

The re-review found real correctness defects in project detection, project lifecycle persistence, Compose path handling, update and provider job ownership, terminal I/O, log startup ownership, Agent cross-project ownership, image import/export, restore compensation, event delivery, metrics boundaries, and Windows packaging.

The source remediation is materially stronger and the backend working tree has passed a complete uncached native Windows Go test run. The current frontend delta has passed focused high-risk tests plus typecheck, lint, and formatting.

An intermediate exact production Wails binary reached the native WebView launch gate and displayed a blank window. Runtime diagnostics captured `Uncaught TypeError: r is not a function` in a generated vendor chunk. The failure was caused by Rolldown max-size vendor splitting creating circular chunks whose CommonJS initialization order was not preserved. This is valuable negative evidence: resource-valid and launchable is not the same as a functioning application.

The strict-execution-order correction is now browser-validated: TypeScript, release-validation build, targeted real-Chromium boot, the full release-UI suite (16 passed, 1 opt-in golden skipped), production build, ESLint, Prettier, and diff check passed in a clean staged frontend copy. The production main application chunk is 498.84 KiB and the largest vendor chunk is 287.26 KiB. RR-2026-029 nevertheless remains open until the corrected source is rebuilt into the exact Wails executable and that native WebView launches cleanly.

The work must **not** yet be called release-complete because the following acceptance evidence was still pending when this report was written:

1. the complete frontend Vitest run after all re-review edits, including the new Agent ownership test file;
2. the corrected final Wails Windows resource build;
3. inspection of the generated PE for architecture, icon, manifest, `FileVersion`, `ProductVersion`, product/company metadata, and Go build metadata;
4. successfully launching that exact corrected executable and exercising startup, provider detection, project detection, logs, terminal, Agent, and shutdown without a WebView runtime exception.

The release decision therefore remains **pending final package acceptance**, not “all review findings are closed.” The original architectural and supply-chain residuals also remain unless this delta explicitly changes their status.

## Reading this delta

- Existing finding IDs keep their original meaning and severity.
- `RR-2026-*` identifies a defect or incomplete remediation found during this re-review.
- “Fixed in working tree” means code and focused regression coverage exist.
- “Validated” means the listed test evidence has passed on the working tree.
- “Pending final gate” means the implementation exists but the final full-suite or packaged-binary proof was not yet available.
- “Residual” means the re-review confirmed the risk still exists; it is not silently downgraded because adjacent work improved.

## Flow-by-flow review matrix

| Flow | What was traced | Finding(s) | Current result | Remaining recommendation |
| --- | --- | --- | --- | --- |
| Application startup | root context, store open/migrate, services, event bridge, runtime controller, shutdown order | BE-050, BE-069, BE-071; RR-2026-010, RR-2026-011, RR-2026-012, RR-2026-030 | Provider installs, update jobs, and backup/restore jobs now have root-owned admission, cancellation, and join behavior; terminal and metrics shutdown were bounded more coherently. | Run a clean-start/dirty-state/close-under-load native smoke against the final binary. Global plan pruning, durable crash reconciliation, and every remaining custom worker still need one documented root-lifecycle contract. |
| Desktop/server trust boundary | production desktop tags, development-only server tags, bind address, privileged services/events | BE-001, BE-002, BE-073, BE-074, BE-076; OPS-001 through OPS-003 | Unsupported remote server mode remains quarantined from stable build/run/release paths and requires an explicit unsafe development tag/acknowledgement with loopback bind. | Do not present this containment as an authenticated remote product. A future server must have a separately designed authenticated/authorized API, origin controls, quotas, and least-privilege service surface. |
| Database migration | migration enumeration, schema pin, SQLite helper registration, rollback/quarantine behavior | BE-005, OPS-029; RR-2026-001 | Migration v12 canonicalizes eligible Windows WSL runtime identities and safely quarantines ambiguous/invalid rows. The embedded latest version is asserted. | Preserve an upgrade fixture for every released schema and test power-loss/failure recovery on a copied real database. |
| Provider detection and selection | Linux, Windows WSL, Colima, existing context, immutable runtime scope | BE-005 through BE-007, BE-018; RR-2026-001 | The Windows WSL runtime and persisted store now use the same versioned `wsl:<lowercase-distro>` identity. This fixes the observed “containers visible, projects missing” class caused by casing aliases. | This is not the complete global project/import-origin identity redesign from `10-project-identity-migration-design.md`. |
| Provider install review/apply | plan creation, expiry, claim, replay, command execution, progress, audit, shutdown | BE-012, BE-013, BE-071; RR-2026-009, RR-2026-010 | Plans are bounded, expired plans are pruned, apply claims before the first command, provider-local copies are discarded, and concurrent/retry replay is rejected. Jobs are root-owned and joined before bus/store close. | Persisted crash recovery and durable operation reconciliation remain broader operation-protocol work. |
| Provider settings | initial load, save serialization, active-provider refresh, WSL/Colima controls | FE-015 through FE-017, FE-067; RR-2026-025 | Previously remediated setting serialization remains. | Colima fields still need the same disabled/pending and Enter-versus-blur deduplication contract as shared settings controls. |
| Compose path translation | workdir, project directory, Compose files, interpolation env files, provider mapper errors | BE-063, BE-080; RR-2026-002 | `ps`, `config`, and ordinary project commands now all use the strict mapper and fail closed on cancellation, mapping error, or blank mapped path. | Add native provider fixtures for UNC paths, WSL mounted drives, case changes, deleted files, and non-ASCII paths. |
| Compose discovery and reconciliation | Docker labels, config enrichment, store publication, overlapping refreshes, cancellation | BE-048, BE-065; RR-2026-003, RR-2026-004 | Same-scope reconciliations are FIFO serialized; different scopes remain concurrent; canceled queued work releases its place. A complete accepted snapshot is authoritative even when a project now has zero services. | The process-global gate should eventually become an explicitly owned detector/service primitive with wait/queue diagnostics. |
| Project import | review/plan, unforget, project upsert, service replacement, failure rollback | BE-014, BE-059; RR-2026-006 | Tombstone removal and imported snapshot publication now share one transaction. A failed save cannot resurrect a forgotten project without its data. | The broader durable-operation/outbox protocol and full content/effect token are still open. |
| Project forget/delete | tombstone creation, scoped deletion, services, mutable update/lineage state, rollback | BE-005, BE-059; RR-2026-006, RR-2026-007 | Forget plus delete is transactional and exact-scope. Failure rolls back both sides. Project-incarnation mutable state is removed or detached so a same-ID reimport/redetection cannot inherit stale lineage, ignored-update policy, current checks, or rollback capability. | Durable historical metrics, backups, audit, and completed update history intentionally remain and need explicit UI/runbook semantics. |
| Project stale cleanup | stale detected rows, imported-row protection, mutable child cleanup, redetection | BE-006, BE-065; RR-2026-007 | Stale detected-project cleanup removes incarnation-owned mutable state before deletion; later redetection starts clean. | Add long-running database-size/retention coverage for the intentionally retained history. |
| Project detail | route/request generations, details, services, actions, lineage, update state | FE-001, FE-007; RR-2026-022 | The existing request-generation ownership remains; failed lineage refresh now preserves last-known-good lineage and exposes a visible error. | Continue decomposing project detail out of the root component and validate all route transitions in native WebView. |
| Lineage persistence | replace project/service lineage, base refs, published update-check references | BE-008, BE-081; RR-2026-005 | Update-check foreign-key references are detached transactionally before mutable lineage replacement. Published history is not falsely rebound to newly inserted lineage IDs. | Persist an explicit parser version/error state if arbitrary frontend support is later expanded. |
| Containers inventory/detail | independent slice generations, actions, bulk selection, hidden/removed rows | FE-007, FE-008, FE-024; RR-2026-021 | Bulk selection is pruned to the currently actionable visible resource set before mutation; a filter cannot cause a hidden selected container to be changed. | Apply one shared actionable-selection primitive to every bulk-capable resource table and test deletion/event races. |
| Container terminal | shell detection, privilege, open, write/paste/resize, close, shutdown | BE-050, BE-056, BE-079, BE-080; FE-013, FE-014; RR-2026-012, RR-2026-020 | Writes are size-bounded, copied, queued, serialized per session, cancellation-aware, and joined during close. New sessions cannot be admitted after stop. Initial enumeration is merged with sessions opened/closed while the request was pending. | Hidden terminal tabs still retain live runtime cost (FE-045). Define suspension/retention policy before large-session use. |
| Images list/detail/actions | list/detail, pull, push, tag, remove, run plan | BE-016 through BE-018, BE-058 | No new defect was found in the already-remediated scoped action paths during this delta. | Keep real-daemon integration coverage for registry authentication and partial external mutation. |
| Registry login and credential use | challenge origin, token/index responses, helper storage, config transaction, authenticated pull/push | BE-003, BE-017, BE-041, BE-042, BE-077, BE-078, BE-083 | Realm trust, response bounds, opaque secret handling, helper fail-closed behavior, authenticated pull, cache/limiter bounds, and Cairn-writer serialization/rollback are remediated. | BE-078 remains partial because an external Docker/config writer does not honor Cairn's lock; preserve compare/reload diagnostics and never claim a cross-process transaction with third-party writers. |
| Image save | Docker stream, destination collision, temporary file, flush/close, publication | BE-059, BE-060, BE-075; RR-2026-015 | Save now stages in the destination directory, checks copy/sync/close, publishes atomically without clobbering an existing file, cleans temporary files, and reports partial publication when cleanup alone fails. | Filesystems without same-directory hard links fail closed; document this constraint and add an operator-facing recovery hint. |
| Image load | selected path, file identity, special files, upload race, daemon JSON stream | BE-060, BE-074; RR-2026-016 | Load accepts only a stable regular file, rejects path/identity/size/modtime changes, bounds bytes/messages/result text, rejects malformed/trailing/non-object/empty responses and Docker error fields, and requires a final `Loaded image:` or `Loaded image ID:` stream record before success/invalidation. | A common renderer file-grant/capability contract remains required for all host-file APIs. |
| Volumes and backup | plan, reserve, job admission/lifetime, backup, restore, overwrite/new target, helper failure, cleanup | BE-030 through BE-036, BE-071, BE-075; RR-2026-014, RR-2026-030 | A failed restore removes a target only when this restore created it, with bounded detached cleanup and explicit partial-state reporting. Active backup/restore jobs are root-owned, canceled/joined by StopAll, rejected after stop, and release a reservation when admission is rejected. | BE-031 through BE-034 and the remaining part of BE-036 stay open: helper immutability/privilege, live consistency classification, fidelity, daemon-side helper cancellation, crash reconciliation, and global plan pruning. |
| Networks | create/delete review, scope, invalidation | BE-059, BE-074 | No new network-specific defect was confirmed in this delta. | Retain partial-resource reporting and add real-daemon collision/retry fixtures. |
| Metrics collection | watcher admission, raw aggregation, retention/downsample, tier stitching, stop | BE-010, BE-011, BE-050, BE-053; RR-2026-008, RR-2026-013 | Read stitching and downsampling now use the same aligned complete-bucket cutoff. Watcher shutdown uses one shared deadline instead of multiplying the timeout by watcher count. | Per-client leases/deduplication and disconnect reclamation remain required for any future multi-client surface. |
| Logs start/follow | frontend request ownership before RPC returns, lines, EOF/error, stop | BE-019 through BE-024; FE-009, FE-022; RR-2026-017, RR-2026-018 | A frontend UUID is registered before `StartLogStream`; backend validates and echoes it on line/EOF/error payloads. Synchronous first lines are retained. EOF uses bounded critical delivery. | Log error remains visible through existing ownership checks, but durable replay and multi-client leases remain open. |
| Log fetch/page/export | bounded fetch, authenticated cursor, export destination/publication | BE-020 through BE-023, BE-060; FE-010 through FE-012, FE-025 | No regression was found in the prior bounded snapshot/cursor/export work. | In-flight export is still not canceled solely because the page unmounts; backend bounds contain it. |
| Update checks | plan/check admission, scheduler, check-all owner, progress, timeout, cancel, shutdown | BE-008, BE-009, BE-043, BE-046, BE-050, BE-071; FE-019; RR-2026-011 | Backend jobs are root-owned, cancelable by job ID, joined on stop, and reject post-stop admission. The frontend has a 15-minute watchdog that cancels a missing-terminal job, releases UI ownership, reports failure, and ignores a late terminal event. | Persist/reconcile jobs across process crash if update operations become resumable. |
| Update apply and rollback | plan rebinding, image/Compose mutation, health observation, rollback trigger, history/audit finalization | BE-044 through BE-046, BE-059, BE-082 | The in-process job lifetime and admission behavior improved in this delta, but no claim is made that the original apply/health/partial-success findings are fully closed. | Capture pre-update container identity/restart baselines, observe restart deltas and a real stabilization window for no-healthcheck services, rebind every plan to current source/Docker state, and persist/query partial outcomes across crash. |
| Update lineage UI | current generation, lineage refresh, failure behavior | BE-008, FE-007; RR-2026-005, RR-2026-022 | Published checks survive lineage replacement with their optional links cleared, and the UI keeps last-known-good lineage on refresh failure. | Explain “historical check no longer linked to current lineage” in diagnostics if exposed to users. |
| Agent status/settings | status/tool catalog refresh, endpoint/model saves, response ordering | FE-056, FE-064; RR-2026-026 | No fix in this delta. | Generation-key every status/tool-catalog refresh and let only the latest request mutate status, provider, endpoint, model, spinner, or error. |
| Agent chat/tool approval | project selection, chat request, parsed tool request, approval, execute, follow-up, cancellation | FE-002, FE-006; RR-2026-019 | Request, pending tool, execution, and follow-up now carry one immutable conversation-generation/project/request owner. Project changes are locked during active request/tool work. Late responses and stale approvals are rejected. | Add a native model-service integration scenario and continue treating this as release-critical because wrong-project tool execution violates explicit user intent. |
| Agent analysis and file editing | context collection/redaction, bounded reads, preview, plan, stable target, atomic write, audit/finalization | BE-037 through BE-040, BE-059, BE-060, BE-062, BE-082 | Endpoint/read/context bounds and atomic, audited file publication are substantially remediated; a durable `started` audit precedes mutation and post-write audit failure is an explicit partial success. | Exact outbound payload preview, generic opaque-secret limits, the common filesystem grant model, and crash-time reconciliation of unfinished audit rows remain open. |
| Audit and notifications | baseline refresh, optimistic intents, events, retention | FE-020, BE-064 | No new functional regression was found in the previously remediated ownership logic. | Audit, notification, and update-history retention/compaction/export policy remains open. |
| Port forwarding | bind semantics, unsupported IPv6, LAN warning, listener/session cleanup | BE-025 through BE-029 | No new code defect was found in this delta. The earlier laptop timeout was attributable to WLAN/client isolation outside Cairn, not missing Cairn IPv6 support. | Keep IPv6 unsupported states explicit; do not widen them. Add global listener/connection/bandwidth quotas before broad LAN use. |
| Runtime event delivery | progress versus terminal semantics, bounded admission, frontend route | BE-004, BE-024; RR-2026-017 | Update, Docker, backup, Compose/project job completion, provider-install progress, terminal close, and log EOF now use bounded critical publication. | This is process-memory reliability, not durable replay. Admission failure is logged; a durable/queryable terminal-operation state remains the architectural closeout. |
| Runtime event decoding | renderer payload casts and malformed host/event data | FE-056; RR-2026-028 | Residual. | Parse every external event/persisted payload through a runtime schema before it enters application state; reject malformed IDs, enums, arrays, and numeric fields. |
| Frontend modal/keyboard flows | command palette over dialogs, focus traps, tabs, tables | FE-033 through FE-038 | Previously fixed tabs/column dialog/focus work remains. | FE-033 remains: stacked command-palette/dialog focus and dismissal ownership need one modal-stack manager. |
| Frontend table virtualization | fixed-height rows, wrapped content, window identity | FE-012, FE-029, FE-044 | Current containment keeps full content reachable through a detail dialog. | True variable-height virtualization remains open; do not claim wrap mode changes row height until measured layout drives offsets. |
| Frontend bootstrap/performance | initial inventory calls, hidden views, root state, chunking | FE-045 through FE-051; RR-2026-027, RR-2026-029 | Independent bootstrap remains substantially serial. Max-size vendor chunking initially broke production startup because circular chunks executed out of order. Strict execution order plus Agent lazy loading now produces a 498.84 KiB main chunk and 287.26 KiB largest vendor chunk and passes real-Chromium production boot/page-error coverage. | Validate the corrected exact WebView binary before final acceptance. Then parallelize independent bootstrap reads, suspend hidden terminal work, split by stable feature boundaries, and continue decomposing the root component. |
| Build dependency resolution | frontend install, Go module mode, binding generation | OPS-013, OPS-014, OPS-025; RR-2026-024 | Build tasks use `npm ci`, remove build-time `go mod tidy`, set readonly Go module mode, and generate a nested `frontend/node_modules/go.mod` after install so root `go ./...` commands cannot traverse npm-owned Go sources. CI/local Go tests now request `./...`. | Run clean `npm ci`, then prove `go list ./...` excludes `frontend/node_modules`; also prove no tracked manifest/lock/module file changes. |
| Production bundle boot | Rolldown splitting, circular vendor chunks, CommonJS initialization, preview/browser errors, native WebView startup | FE-051, FE-061, OPS-017, OPS-024; RR-2026-029 | The intermediate exact binary failed with a blank window and `Uncaught TypeError: r is not a function`. The working tree enables `strictExecutionOrder`, forbids reuse of a stale preview server, asserts `#root` boots, and fails every release-UI test on uncaught `pageerror`. Corrected release-validation/production builds, real-Chromium boot, and the full release-UI suite now pass. | Pending: exact Wails rebuild from the corrected source and native WebView launch with no runtime exception. |
| Windows resources | artwork sync, icon generation, `.syso`, manifest, VersionInfo, architecture | OPS-017, OPS-028, OPS-037; RR-2026-023, RR-2026-029 | The working tree couples asset sync/icon generation to `.syso`, supplies numeric/string file/product versions, and adds a fail-closed PE resource checker. An intermediate candidate reached WebView launch but failed in bundled JavaScript, proving PE-resource checks alone are insufficient. | Final proof is pending: rebuild the corrected bundle through Wails, inspect that exact PE, then successfully launch it. Do not substitute a plain `go build` or a resource-only check. |
| Release artifact validation | expected version propagation, installer presence, binary metadata | OPS-004 through OPS-008, OPS-012, OPS-017, OPS-032, OPS-035, OPS-036 | Windows artifact validation now receives the expected release version and invokes the PE resource checker. | Exact-SHA gates, isolated/fail-closed signing, immutable publication, installed-payload validation, and signed provenance remain release blockers. |

## Delta finding register

### Fixed in the working tree and covered by passing tests

| ID | Severity | Related original finding(s) | Exact defect found | Remediation and evidence |
| --- | --- | --- | --- | --- |
| RR-2026-001 | High | BE-005, BE-006, BE-007 | Windows WSL runtime identity lowercased the configured distro, while durable project scopes could retain display casing. Docker calls could work against the active distro while project queries used a different scope and returned no projects. | One versioned canonicalization function is shared by the provider and migration v12. The migration moves only ownership-valid rows, drops reconstructible noncanonical caches, collapses safe aliases, and demotes ambiguous update generations. Runtime-scope and migration regressions pass in the full Go suite. |
| RR-2026-002 | High | BE-063, BE-080 | Strict Compose path translation was used for verified config, but ordinary `ps`, `config`, and project actions could silently keep host paths when mapping failed. | Every project command now uses the context-aware strict mapper and fails closed. Focused Compose tests cover workdir, project directory, Compose files, env files, mapper errors, blank results, and cancellation; the full Go suite passes. |
| RR-2026-003 | High | BE-065 | Two reconciliations for the same runtime scope could finish in reverse order and commit the older complete snapshot last. | A cancel-safe FIFO scope gate serializes same-scope reconciliation and preserves cross-scope concurrency. Ordering, concurrent-scope, and canceled-waiter tests pass. |
| RR-2026-004 | High | BE-065 | A full detected snapshot containing a project with zero current services did not necessarily clear the project’s previously stored services. | Accepted detected projects are authoritative service-replacement targets even when the service slice is empty. Regression and full-store tests pass. |
| RR-2026-005 | High | BE-008, BE-081 | Replacing mutable lineage could collide with published update-check foreign keys or tempt historical checks to appear linked to newly inserted lineage. | Optional lineage/base-ref links are detached in the same transaction before replacement. Project-, service-, and exact-scope regressions pass. |
| RR-2026-006 | High | BE-014, BE-059 | Import used separate “unforget” and save operations, while removal used separate tombstone and delete operations. A later failure could leave contradictory visibility/data state. | Import/unforget/save and forget/delete now each use one scoped transaction. Trigger-injected rollback tests prove both sides restore together. |
| RR-2026-007 | High | BE-005, BE-059, BE-064 | Removing or aging out a project left incarnation-owned lineage, current checks, ignored rules, or rollback state that could attach to a later project reusing the same ID. | Scoped removal clears mutable update/lineage policy state, detaches unfinished history, disables rollback on retained completed history, and preserves intentional audit/backup/metrics history. Reimport and redetection regressions pass. |
| RR-2026-008 | High | BE-010 | Retention downsampled only complete buckets, but query tier stitching used unaligned wall-clock cutoffs. Non-aligned runs could create a gap or select an incomplete coarse bucket. | Both retention and reads now use the same UTC-truncated complete-bucket boundary. Non-aligned retention/query regression passes. |
| RR-2026-009 | High | BE-013, BE-071 | Provider-install plans could remain replayable after a failed step, be claimed concurrently, outlive expiry, and grow without a manager-level bound. | Manager claims before commands, prunes expiry, caps pending plans at 64, and discards provider-local copies. Failure replay, concurrent claim, expiry, and cap tests pass. |
| RR-2026-010 | High | BE-012, BE-071 | Provider install execution used a detached request context without an application-owned cancel/join boundary. Shutdown could close events/store while a privileged installer still ran. | `ProviderInstallLifecycle` owns admission and jobs under the root context and joins before bus/database close. Lifecycle cancellation/join and progress tests pass. |
| RR-2026-011 | High | BE-043, BE-046, BE-050, BE-071; FE-019 | Update scheduler/jobs were not fully joined and the frontend could remain permanently busy when no terminal progress arrived. | Backend adds root cancel/join, per-job cancel, post-stop rejection, and plan restoration on rejected admission. Frontend adds a 15-minute watchdog, cancellation, ownership release, visible failure, and late-event rejection. Backend and App regressions pass. |
| RR-2026-012 | High | BE-050, BE-056, BE-074; FE-013 | Concurrent terminal writes could interleave on one stream, one write had no byte cap, callers could not cancel queued/blocking work, and sequential stop multiplied close latency. | Per-session bounded serialized writers copy at most 1 MiB per request, honor cancellation, and join on close; StopAll closes sessions concurrently and rejects new admission. Terminal regressions and full Go suite pass. |
| RR-2026-013 | Medium | BE-050 | Metrics shutdown waited up to the full watcher timeout once per watcher, turning a nominal bound into `watcher count × timeout`. | All watcher waits share one absolute deadline. Focused regression and full Go suite pass. |
| RR-2026-014 | Medium | BE-035 | Failed restore into a newly created target volume left partial state, while cleanup must never delete a pre-existing overwrite target. | Cairn records whether this job created the target, compensates only that target, detaches cleanup from request cancellation with a 15-second deadline, and reports cleanup failure as an explicit partial resource. Four focused regressions and full Go suite pass. |
| RR-2026-015 | High | BE-059, BE-060, BE-075 | Image save wrote directly to the requested destination, could truncate an existing file, and did not make copy/sync/close/publication one fail-closed contract. | Same-directory staging, checked flush/close, atomic no-clobber hard-link publication, temp cleanup, and partial-publication reporting were added. Focused Docker tests and full Go suite pass. |
| RR-2026-016 | High | BE-060, BE-074 | Image load accepted unstable/special paths, did not revalidate identity through upload, read an unbounded daemon response, and could treat malformed/error or progress/status-only EOF as success. | Stable regular-file validation and post-upload reinspection were added, along with byte/message/result limits, strict JSON/error semantics, and a requirement for a final `Loaded image:` or `Loaded image ID:` stream record before success or object invalidation. Directory, symlink, replacement, malformed, trailing, empty, error, progress-only, oversized, and message-count regressions pass. |
| RR-2026-017 | High | BE-024 | The reliable critical mailbox existed, but user-visible terminal/completion producers still used the lossy path. | Update, Docker, backup, Compose/project job completion, provider-install progress, terminal close, and log EOF now use a two-second bounded critical admission helper and log failure. Bus, logs, services, backup, Docker, terminal, and update tests pass. |
| RR-2026-018 | High | FE-009, BE-024 | Even synchronous frontend owner registration could not match a line emitted before `StartLogStream` returned the backend stream ID. | The frontend registers a UUID client token before RPC; backend validates/canonicalizes and echoes it on line/EOF/error events; the stream ID is attached after return without dropping token-owned events. A regression emits a line synchronously inside the RPC mock before promise resolution; focused frontend and log-service tests pass. |
| RR-2026-019 | Critical | FE-002, FE-006 | The earlier Agent ownership fix did not bind the entire chat → parsed tool request → user approval → tool execution → follow-up chain to one project/conversation generation. A delayed request could expose or execute a tool for a different visible project. | Immutable owner metadata now travels through every stage, project switching is locked during request/tool work, and late response/stale approval checks fail closed. Three focused ownership regressions pass, including cross-project approval and conversation invalidation. |
| RR-2026-020 | High | FE-014, FE-064 | `ListTerminalSessions` could resolve after a newly opened session and replace the whole local list, making the new live session disappear from the UI. | Initial results merge with sessions added/removed while enumeration was pending and ignore unmounted completions. Focused terminal lifecycle regression passes. |
| RR-2026-021 | Medium | FE-024 | Filtering a bulk table could leave hidden container IDs selected and mutate them through a visible bulk action. | Selections are pruned against the visible/actionable set before the action; the regression proves only the remaining visible container is sent. |
| RR-2026-022 | Medium | FE-007 | Lineage refresh set loading state by replacing last-known-good content and converted failure to an empty-looking result. | Refresh preserves content, tracks status/error separately, and renders the failure while keeping the known lineage. Focused App regression passes. |
| RR-2026-030 | High | BE-036, BE-050, BE-071 | Backup and restore jobs could be admitted without one manager-stopped gate and complete after shutdown closed dependent event/store resources; a consumed archive reservation could also remain held if job admission was rejected. | `backups.Manager` now owns a root context, stopped admission flag, job WaitGroup, and cancel/join StopAll path. Post-stop work is rejected, active workers are joined, and failed admission releases the reservation. StopAll-blocks-until-exit and post-stop-admission regressions pass, followed by full Go, targeted race, and Go vet runs. |

### Implemented but pending final packaging acceptance

| ID | Severity | Related original finding(s) | Exact defect found | Working-tree remediation | Required proof before closure |
| --- | --- | --- | --- | --- | --- |
| RR-2026-023 | High | OPS-017, OPS-028, OPS-037 | Existing `bin` executables could be runnable but contain blank/missing Windows VersionInfo and no trustworthy resource acceptance gate. A plain Go rebuild is therefore not an acceptable Cairn release build. | The Windows task synchronizes source artwork, generates `.ico`, generates `.syso` with icon/manifest/info, supplies numeric and string file/product versions, and invokes `scripts/check-windows-binary.ps1`. Its portable byte-level PE parser works under PowerShell on native and cross-build hosts and validates machine type, icon image/group, VersionInfo, product/company/description, manifest identity/version, `asInvoker`, and per-monitor-v2 DPI awareness. | Build through the final Wails task with the stamped version/commit/date; make the checker pass on that exact PE; inspect Go build metadata; launch it and exercise native flows. |
| RR-2026-024 | High | OPS-013, OPS-014, OPS-025 | Build tasks could reuse stale `node_modules`, run `npm install`, run `go mod tidy` during generation/build, rely on separately invoked asset synchronization, omit version/commit/date metadata in cross builds, and let root Go commands traverse npm-owned `.go` files. | Frontend install uses `npm ci`; a postinstall hook creates a nested `node_modules/go.mod` boundary; CI/local tests use `go test ./...`; binding/build steps use readonly module mode and no build-time tidy; asset sync is a dependency of resource generation; cross builds receive version/commit/date; release artifact checks receive the expected version. | Run clean `npm ci`; prove `go list ./...` excludes `frontend/node_modules`; run the complete final build and prove no tracked manifest/lock/module file changes. |
| RR-2026-029 | High, release-blocking until native acceptance | FE-051, FE-061, OPS-017, OPS-024 | Rolldown vendor `maxSize` splitting created circular production chunks whose CommonJS shims ran out of order. Development/focused unit checks stayed green, while the exact Wails WebView rendered a blank window with `Uncaught TypeError: r is not a function`. The release-UI suite also did not globally fail on uncaught page errors and could reuse a stale preview server locally. | Rolldown `strictExecutionOrder`, a dedicated production-bundle boot assertion, global Playwright `pageerror` failure, current-preview enforcement, and a Linux CI release-UI gate were added. TypeScript, both release-validation and production builds, targeted real-Chromium boot, full release UI (16 passed, 1 opt-in golden skipped), ESLint, Prettier, and diff check pass in a clean staged frontend copy. | Pending only the package-level proof: rebuild the exact Wails executable from this corrected source, verify its PE/build metadata, launch its native WebView without an uncaught exception, and exercise core flows. |

## Confirmed residual issues and recommendations

These are not erased by the fixes above.

| ID or original finding | Severity | Residual | Recommendation / exit criterion |
| --- | --- | --- | --- |
| RR-2026-025 | Medium | Colima setting inputs are not disabled while saves are pending and the profile Enter handler can be followed by blur, issuing a duplicate save/refresh sequence. | Use the shared serialized setting control, suppress blur after a committed Enter, expose pending/error state, and add delayed/rejected Enter-plus-blur tests. |
| RR-2026-026 | Medium | Agent status/tool-catalog refreshes have mount cancellation but no shared request generation. A slow older refresh can overwrite newer status/settings and clear a newer spinner/error. | Add monotonic request ownership covering success, error, and `finally`; test reordered initial/manual/post-save refreshes. |
| RR-2026-027 | Medium | Independent initial inventory/bootstrap work remains substantially serial, increasing time-to-usable and making one slow slice delay later views. | Start independent reads concurrently under a bounded coordinator; retain per-slice last-known-good/error ownership; measure cold-start budgets. |
| RR-2026-028 / FE-056 | Medium | Several Wails event and restored-state boundaries still use TypeScript casts instead of runtime validation. Malformed payloads can enter trusted UI state. | Centralize schemas and negative tests for every event and persisted version; reject malformed values before state mutation. |
| BE-002, BE-074 | High | Service-local caps do not form one application-wide request/deadline/memory/admission budget. | Define one request-budget object and telemetry contract propagated through every public service and background operation. |
| BE-005 | High | WSL casing is fixed, but project IDs are still globally shaped and durable import-origin identity/full resolver migration remain incomplete. | Implement the `10-project-identity-migration-design.md` resource identity, collision resolver, migration, aliases, and rollback plan. |
| BE-014 through BE-016, BE-045 | High | Dangerous operation plans still lack one universal immutable normalized resource/effect token across all mutation families. | Complete the unified durable operation protocol and prove single-use, expiry, current-state rebinding, idempotency, partial success, and audit/outbox behavior. |
| BE-044 through BE-046 | High | Update job lifetime improved, but health rollback can still confuse historical restart count with a new failure, no-healthcheck stability is not observed with a strong contract, plan rebinding remains incomplete, and crash-time finalization/reconciliation is not durable. | Capture identity plus restart baseline/delta, require a real stabilization interval, revalidate current Docker/source state at apply, and persist a queryable operation state machine/outbox. |
| BE-024 | High | Critical publication is bounded and substantially migrated but remains in-memory; a crash or undrained subscriber can still leave the UI without queryable terminal truth. | Persist operation terminal state/outbox and make the UI reconcile by job ID after timeout/restart. |
| BE-025 | High | Explicit `0.0.0.0` WSL forwarding remains LAN-capable and globally unbudgeted. WLAN/client isolation can still block peer access even in one VLAN; that is network policy, not a Cairn listener defect. | Add global/per-forward quotas and document Omada SSID/client-isolation requirements; retain fail-closed unsupported IPv6 behavior. |
| BE-031 through BE-034, BE-036 | High/Medium | Backup job admission/cancel/join is fixed, but helper supply chain/privilege, live consistency, fidelity, daemon-side helper cancellation, crash reconciliation, and global abandoned-plan pruning remain incomplete. | Version/pin a least-privileged helper, expose crash-consistency class, define fidelity, add daemon cleanup/reconciliation, and implement global plan expiry/pruning. |
| BE-037, BE-039 | High | A malicious same-user loopback Agent service can receive allowed context; generic opaque secrets cannot be guaranteed redactable; exact outbound payload preview is absent. | Add an exact final payload preview/consent boundary and keep endpoint trust wording explicit. |
| BE-053, BE-019 | High/Medium | Metrics/log budgets are process-wide, not authenticated-client leases with disconnect reclamation. | Required before any supported multi-client/server surface. |
| BE-060 | High | Some renderer-selected filesystem APIs remain outside a unified OS-dialog grant/capability model. | Replace raw paths with short-lived purpose-bound host grants and revalidate stable file identity at use. |
| BE-059, BE-060 | Medium portability residual | Atomic no-clobber image/backup publication intentionally requires same-directory hard-link support and fails closed when the destination filesystem cannot provide it. This is safe but can make otherwise valid removable/network filesystems unsupported. | Document supported destination filesystem semantics, expose the exact recovery hint, and add a separately reviewed safe publication strategy before claiming broader filesystem support. |
| BE-064 | Medium | Audit, notifications, and update history still lack complete retention/compaction/export policy. | Define per-table age/count/size bounds, user controls, vacuum behavior, and recovery/export tests. |
| BE-071 | Medium | Provider-install, update, and backup/restore jobs now have in-process root ownership, but custom work is not yet universally normalized and crash reconciliation remains feature-specific or absent. | Maintain one root operation registry with admission, cancel, join deadline, diagnostics, persistence class, and shutdown order. |
| FE-033 | High | Command palette stacked over another dialog does not yet have one modal-stack focus/dismissal owner. | Implement a modal manager; Escape closes only the top layer; focus/inert state restores to the correct underlying owner. |
| FE-044 | High | DataTable virtualization still assumes fixed height while wrapped content implies variable height. | Use measured/variable rows or prohibit wrapping in the virtual surface and expose full content separately. |
| FE-045 | Medium | Hidden terminal tabs remain mounted/live. | Define pause, retention, output, and process lifecycle policy; test memory/CPU with the maximum session count. |
| FE-047, FE-051 | High/Medium | High-frequency state remains concentrated in the very large root component. The corrected split bundle passes browser production boot and reports a 498.84 KiB main chunk plus 287.26 KiB largest vendor chunk, but it has little budget headroom and native corrected-binary acceptance remains pending. | Prefer stable feature/route boundaries over size-only splitting, retain strict execution-order protection where circular chunks are possible, continue lazy-loading heavy terminal/chart/editor code, and enforce both compressed chunk budgets and production boot tests in CI. |
| OPS-004 through OPS-008 | High | Exact-SHA gate enforcement, signing isolation, fail-closed stable signing, and immutable assets remain release blockers. | Release only after the tagged commit’s required checks succeed; separate signing from dependency/build execution; sign installed payloads; prohibit replacement. |
| OPS-010, OPS-012 | High | Vulnerability waivers and third-party actions/images/tools still need immutable pinning and governed reachability-based exceptions. | Pin by digest/SHA and require owner, rationale, reachable component, expiry, and renewal evidence for every waiver. |
| OPS-014 | High, pending validation | The earlier full Go run still crossed into `frontend/node_modules/flatted/golang/pkg/flatted`. A working-tree postinstall hook now creates a nested module boundary, and CI/local tests were broadened to `go test ./...`, but the clean-install boundary proof is not yet recorded. | Run clean `npm ci`, verify the generated boundary content, prove `go list ./...` contains no `frontend/node_modules` package, and retain that assertion in CI. |
| OPS-017 | High | The new Windows checker does not by itself validate NSIS installed payload, uninstall behavior, Authenticode chain, or macOS/Linux package semantics. | Add clean-VM install/uninstall and installed-file checks for each package/architecture. |
| OPS-021 through OPS-024 | Medium | Project-wide recurring race/coverage, timeout/evidence preservation, isolated Docker integration, and native-webview coverage remain incomplete. | Add explicit CI jobs with bounded time, artifacts, thresholds, and platform-native smoke. |
| OPS-032, OPS-035, OPS-036 | Medium/High | Checksums/SBOM are not signed provenance; release cancellation/provenance and workflow-token/credential scope remain broader than required. | Emit signed attestations tied to immutable source/artifact digests and least-privilege publication identities. |
| OPS-034 | Medium | `go.mod` and hosted CI/release pin Wails CLI/runtime `v3.0.0-alpha2.103`, while the locally installed `wails3` reports `v3.0.0-alpha2.106`; the frontend package is `@wailsio/runtime` `3.0.0-alpha.79`. A successful build with an unrecorded CLI skew is not release-equivalent evidence. | Build with the repository-pinned CLI or record an explicit protocol/binding/native smoke proving the intentional skew; verify regenerated bindings are unchanged and include the exact CLI/runtime versions in build evidence. |

## Operational and environment items outside source remediation

These must be reported separately from application bugs because changing Cairn source cannot prove or override them:

| Item | Current interpretation | Operator action / proof |
| --- | --- | --- |
| Omada SSIDs in one VLAN | Different WLANs can still apply client isolation, guest policy, ACL, or MLO-specific policy even when clients receive addresses from the same VLAN. That can make a laptop time out while Cairn's IPv4 listener is correct. | Compare WLAN advanced settings and applied ACL/profile policy; disable client/guest isolation where peer access is intended; verify direct IP ping/TCP in both directions. |
| Ollama reachability | Cairn cannot make Ollama listen on a LAN interface or override host firewall/WLAN policy. A local-only Ollama bind will remain unreachable from the laptop. | Verify Ollama's configured listen address and port, host firewall/Norton rule, Omada policy, and direct TCP reachability independently of Cairn. |
| Unsupported IPv6 published binds | Cairn intentionally does not create a Windows listener for unsupported IPv6 bind semantics and does not widen them into IPv4. This message is an application limitation, not proof that the LAN lacks IPv6. | Use a supported IPv4 bind when appropriate, or implement/test exact IPv6 listener semantics as a separate feature. Keep failure visible and fail closed. |
| Norton 360 / endpoint protection | Endpoint protection can quarantine, suspend, or delay newly generated unsigned executables and Go test binaries. That is not automatically a Cairn defect, but it can invalidate build/test timing and launch observations. | Check security history, sign release outputs, and scope any allow rule narrowly to the trusted artifact/build directory. Do not treat disabling protection globally as a release procedure. |
| Signing/notarization credentials | Local source review cannot prove Windows Authenticode, macOS signing/notarization, timestamping, or secret isolation without the release identities and hosted workflow. | Run isolated signing jobs and validate trust chains/timestamps on clean machines before stable publication. |
| Clean-machine installers | A verified standalone PE does not prove NSIS/MSIX installation, upgrade, uninstall, WebView bootstrap, Start-menu icon, or installed-file signature behavior. | Exercise installers in clean VMs for every supported architecture and retain logs/screenshots/hash evidence. |
| macOS/Linux native GUI packages | The current Windows session cannot prove native macOS WebKit/signing behavior or Linux desktop/package integration. | Require platform-native CI and clean-machine launch/package smoke before calling those artifacts supported. |
| Real Docker/WSL integrations | Several real-daemon and WSL tests are environment-gated. Passing unit/fake-provider tests does not prove daemon cancellation, real archive behavior, WSL path mapping, or listener behavior on this machine. | Run the documented real Docker and WSL integration matrix in prepared CI/hosts and retain command/log evidence for the release SHA. |
| Historical local backups | Older `.bak-*` files may retain data that newer migrations scrub from the live database; automatic deletion/rewrite would be unsafe. | Document retention and let the user rotate/delete old backups deliberately after verifying recovery needs. |

## Validation evidence available at this checkpoint

### Passed at the pre-final source-remediation checkpoint

- Native Windows uncached `go test -work ./... -count=1` passed for the root, every internal package, and repository tools at that checkpoint.
- Focused frontend Vitest passed **4 files / 171 tests** covering App, Agent ownership/accessibility, and terminal lifecycle before the final lazy-loading/bundle correction.
- Frontend TypeScript `--noEmit`, repository ESLint, and Prettier check passed before the final lazy-loading/bundle correction.
- Focused Docker plus backup tests passed.
- Focused log-service plus service-layer tests passed.
- The complete Go run includes the new migration, store, Compose, provider, update, terminal, metrics, Docker image, backup compensation, event publication, and log ownership regressions.
- After the final backup-lifecycle and image-load terminal-success changes, `go test ./...`, targeted race-enabled tests for the affected backend, and `go vet ./...` all passed.
- For the corrected lazy-loading/bundle change, a clean staged frontend copy passed TypeScript, release-validation Vite build, targeted real-Chromium boot, the full release-UI suite (16 passed, 1 opt-in golden skipped), production Vite build, ESLint, Prettier, and `git diff --check`. The production output measured 498.84 KiB for the main application chunk and 287.26 KiB for the largest vendor chunk.

### Failed intermediate acceptance evidence

- An intermediate production Wails executable launched its native window but rendered no application UI.
- WebView diagnostics reported `Uncaught TypeError: r is not a function` in a generated vendor chunk.
- The failure was traced to Rolldown max-size splitting and circular CommonJS-dependent chunks without strict execution ordering.
- That intermediate binary is rejected even if its PE resources and metadata pass. It must not be described as the final working rebuild.

### Pending

- Repeat backend evidence only if backend source changes after the recorded final run.
- Complete frontend Vitest run after all final edits, including the new Agent ownership test file; record the actual file/test totals.
- Final-tree frontend TypeScript, ESLint, and Prettier checks outside the staged-copy proof.
- Clean corrected Wails Windows build with stamped `APP_VERSION`, `GIT_COMMIT`, and `BUILD_DATE`.
- Passing `scripts/check-windows-binary.ps1` on the final executable.
- Final executable Go build-info inspection.
- Successful native WebView launch and flow smoke of the exact corrected executable.
- Clean `npm ci` followed by proof that `go list ./...` excludes `frontend/node_modules`.
- Installer creation/install/uninstall/signature validation was not part of this local binary-rebuild request unless explicitly run later.

### Dependency-boundary evidence awaiting refresh

The earlier full Go run still discovered `frontend/node_modules/flatted/golang/pkg/flatted`. The working tree now installs a nested `frontend/node_modules/go.mod` boundary and broadens CI/local tests to `go test ./...`. OPS-014 remains pending until a clean `npm ci` followed by `go list ./...` proves that no npm-owned Go package crosses the root module boundary.

## Final acceptance checklist

The remediation can be handed off as the requested working snapshot only when all of the following are true:

- [ ] Complete frontend suite passes on the final tree.
- [ ] TypeScript, ESLint, Prettier, complete Go test, and Go vet pass on the same final tree.
- [x] Corrected Vite production build passes; chunk sizes and the circular-order root cause are recorded.
- [x] Release-UI production boot test sees a non-empty root and zero uncaught `pageerror` events from a freshly built `dist` (16 passed, 1 opt-in golden skipped).
- [ ] Clean `npm ci` generates the nested Go module boundary and `go list ./...` contains no `frontend/node_modules` package.
- [ ] Wails bindings are generated without changing dependency manifests.
- [ ] The Wails CLI version matches the repository pin (`v3.0.0-alpha2.103`) or an intentional skew has explicit compatibility evidence.
- [ ] Canonical artwork is synchronized and the `.ico` is regenerated.
- [ ] Wails generates the architecture-correct `.syso`.
- [ ] Production executable is rebuilt through the Wails Windows task, not plain `go build`.
- [ ] PE checker verifies architecture, icon resources, VersionInfo, manifest identity/version, execution level, and DPI awareness.
- [ ] Go build info reports the expected Go toolchain and source build metadata.
- [ ] The exact corrected binary launches and its WebView reports no uncaught JavaScript error.
- [ ] Startup detects the active provider and projects, including the canonical WSL scope.
- [ ] Project detail, container action, logs, terminal, Agent ownership, update timeout/cancel, and shutdown smoke succeed.
- [ ] `git diff --check` passes and generated artifacts/source changes are reviewed.
- [ ] Only then is the binary called the final working rebuild.

## Bottom line

The re-review fixes a large, concrete set of correctness bugs, including the project-detection regression the user observed. It also closes the most dangerous newly exposed frontend gap: Agent tool approval was not fully bound to its originating project.

The intermediate blank-window failure demonstrates why the final bundle and native WebView smoke are mandatory: a binary can be correctly stamped and still be unusable. The strict-order correction is now production-browser validated; RR-2026-029 remains package-level open only until the corrected exact Wails executable passes native WebView launch and core-flow smoke.

This work does **not** erase the original release blockers or substitute browser checks for packaged-binary proof. The source tree is at a strong remediation checkpoint; final “done” status depends on the pending full frontend/vet/build/resource/native-launch gates and on whether the user intends “done” to include the still-open architectural and release-supply-chain findings listed above.
