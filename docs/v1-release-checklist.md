# v1 Release Checklist Evidence

Source of truth: `dev-docs/06-testing.md section 9`. This file is a release evidence ledger, not a replacement for the normative checklist.

Status legend:
- `green`: current evidence is sufficient for the checklist item.
- `in_progress`: implementation exists, but required release evidence is incomplete.
- `blocked_by_platform`: implementation exists, but the remaining proof requires a platform VM or manual installed-app run that is not available in this environment.

| Checklist item | Status | Current evidence | Remaining proof before v1.0 |
|---|---:|---|---|
| All P0 features pass on Linux native | blocked_by_platform | Ubuntu 24.04 CI run 27502520080 is green for commit `0c2a928`, including lint/unit, real Docker reconnect/logs/metrics/terminal/backup/registry/tag-push integrations, package smoke, Ubuntu `.deb` install/uninstall, Debian stable container `.deb` install/uninstall, release DB upgrade fixture, expanded security suite, release validation smoke with the seeded UI performance fixture, v1 checklist/manual-matrix evidence-ledger validation, and soak-checker smoke. Local 2026-06-15 release validation passed `checklist,manual-matrix,soak-checker,upgrade-fixtures,security,performance,ui-release,wsl-provider`. The real 24 h WSL/Linux active-stream soak completed with duration `24h0m0s`, logs `1728027`, stats `86401`, terminal bytes `146870`, dashboard reads `2881`, baseline goroutines `3`, peak `35`, final `8`. | Close the Ubuntu installed-app desktop, AppImage, permission, rootless, and Debian stable desktop/AppImage/rootless rows in `docs/manual-platform-validation.md` on real clean VMs. |
| All P0 features pass on Windows WSL | blocked_by_platform | Local dedicated `cairn-dev` WSL evidence covers provider setup, Docker SDK over WSL stdio, backup/restore, registry tag/push, and update/rebuild smokes without Docker Desktop contexts. `./scripts/run-release-validation.ps1 -Suite wsl-provider` passed locally on 2026-06-15, preflighting the dedicated distro, asserting Go 1.26.4, verifying Windows Docker context immutability, and running WSL SDK connection, backup/restore, registry tag/push, and update/rebuild smokes. Windows CI run 27499807123 is green for unit/frontend/package smoke. | Clean Windows 11 VM onboarding from WSL absent/partial states, full WSL provider integration suite, installed app smoke, and uninstall cleanliness. |
| All P0 features pass on macOS (Colima + existing context) | blocked_by_platform | macOS CI run 27499807123 is green for unit/frontend/package smoke and dmg/app artifact checks. Colima/existing-context behavior is covered by parser, provider, service, and frontend fixture tests. | Apple Silicon Colima VM onboarding/runtime/update/rebuild/tag-push smoke, existing Docker Desktop/OrbStack context switch, host/backend terminal smoke, and uninstall cleanliness. |
| Update + lineage + registry-account test suite green (section 5 all 14 cases) | green | Phase 8 exit evidence records all 14 normative cases across registry, lineage, updates, Docker, store, and frontend tests; Ubuntu CI includes real registry auth and tag/push integrations; Windows WSL `cairn-dev` registry and update/rebuild smokes are green. | macOS Colima runtime repeat remains a platform-matrix row, but the normative automated suite is green. |
| Visual-regression and axe accessibility suites green | green | Release UI smoke runs axe on every route plus command palette, notification center, import modal, and daemon-stopped degraded states; screenshots are compared against committed Windows/Linux goldens with a 0.2 percent pixel threshold. CI run 27502520080 passed Ubuntu release validation smoke, and local Windows `npm run test:release-ui` passed the current 16-check suite on 2026-06-15 after refreshing reviewed Windows and Linux goldens for the internal scroll-region capture fix. | Repeat intentional golden updates only with `CAIRN_UPDATE_VISUALS=1` on the target platform and reviewed PNG diffs. |
| No destructive action without confirmation (section 6 suite green) | green | `scripts/run-release-validation.ps1 -Suite security` covers command-plan enforcement, typed-name requirements, restore/update/container/project paths, project/container plan stores, expiry/context-cancel behavior, and backend locking of `security.confirm_destructive`. Ubuntu release validation smoke passed in CI run 27502520080. | None currently known. |
| No plaintext secrets anywhere (redaction suite green) | green | Security suite covers registry login via `--password-stdin`, audit/log/DTO redaction, auth env masking, and no Cairn persistence of registry secrets. | None currently known. |
| No Docker TCP exposure configured by Cairn | green | Security review verifies Cairn never configures Docker TCP exposure and only warns on existing unencrypted `tcp://` contexts. | Manual context matrix should verify warning visibility on a real unencrypted remote context. |
| Performance targets met at seeded scale (section 7) | green | Release validation smoke covers seed-scale backend performance; frontend tests cover route/search/dashboard behavior; browser-level release UI seeded fixture passes the v1 scale target with 100 containers, 500 images, 200 volumes, 20 networks, 10 projects, and 5,000 virtualized log lines; Ubuntu real Docker stream integrations are green; the 24 h active-stream soak completed without goroutine growth beyond threshold. | None currently known. |
| Installers install/uninstall cleanly on all 3 OS | blocked_by_platform | CI run 27502520080 package smoke is green for NSIS, AppImage, `.deb`, dmg, macOS app artifact checks, Ubuntu `.deb` install/uninstall, and Debian stable container `.deb` install/uninstall. The Linux package smokes verify binary, desktop file, icon, no Docker package deps/group mutation, clean removal, and `ldd` dependency resolution inside Debian stable. | Installed-app smoke and uninstall cleanliness on clean Windows 11, macOS, Ubuntu desktop, Debian stable VM, and AppImage launch on clean Linux. |
| App handles Docker-stopped state gracefully on all pages | green | Browser release smoke includes daemon-stopped degraded fixture across every release route: global degraded banner, stale cached-data watermark, no serious axe violations, disabled container mutation, and no log/stats stream startup. CI release validation smoke passed on Ubuntu in run 27502520080, and local Windows `npm run test:release-ui` passes the current 16-check suite. | Installed-app Docker-stopped/degraded passes on Ubuntu desktop, Debian, Windows WSL, and macOS Colima/existing context remain platform-matrix sign-off rows. |
| Crash-free soak: 24 h run with active streams, zero goroutine leaks (pprof check) | green | Real WSL/Linux 24 h soak `phase10-24h-20260614T071038Z` completed successfully and was validated with `scripts/check-soak-status.ps1 -RequireComplete`: duration `24h0m0s`, logs `1728027`, stats `86401`, terminal bytes `146870`, dashboard reads `2881`, baseline goroutines `3`, peak `35`, final `8`, exit code `0`. `scripts/test-soak-status-checker.ps1` also proves the checker rejects short, failed, and leaky completed runs. | None currently known. |

## Current blockers

- Clean Windows 11 WSL VM matrix and installed-app smoke.
- macOS Apple Silicon Colima/existing-context VM matrix and installed-app smoke.
- Debian stable desktop/AppImage/degraded/rootless matrix.
- Manual installed-app Docker-stopped behavior on real desktop apps.
- Production signing/notarization credentials for final public artifacts.
