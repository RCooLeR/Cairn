version: "2"

linters:
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused
  exclusions:
    generated: lax
    paths:
      - ^frontend/node_modules/
      - ^frontend/dist/
      - ^bin/
      - ^build/

run:
  timeout: 5m
  tests: true
