# Cairn v1.0 Progress

Source of truth: `dev-docs/`, especially `dev-docs/07-development-plan.md`.

Status legend: `todo`, `in_progress`, `green`, `blocked`.

| Phase | Step | Status | Evidence | Notes |
|---|---|---:|---|---|
| 0 | 0.0 Dev environment (Windows machines) | green | `wsl -l -v` shows `cairn-dev` running as WSL2 alongside Docker Desktop's internal distro; `wsl -d cairn-dev -- test -d /run/systemd/system` passes; Docker Engine 29.5.3, Compose 5.1.4, and Buildx 0.34.1 are installed inside `cairn-dev`; `/usr/bin/docker` resolves to `/usr/bin/docker`, not Docker Desktop's WSL proxy; Windows Docker Desktop contexts remain untouched. | All local Windows Docker work must target `cairn-dev`, never Docker Desktop or `desktop-linux`. Clean Win11 VM setup/failure cases remain in `docs/manual-platform-validation.md`. |
| 0 | 0.1 Repo & toolchain | green | `npm audit --audit-level=moderate`; ESLint; Vitest; `tsc --noEmit`; Vite production build; `go test . ./internal/...`; `go vet . ./internal/...`; `go build ./...`; `wails3 build`; `golangci-lint v2.12.2 run --timeout=5m`; CI run 27431749444 green on Ubuntu 24.04, Windows, macOS; `wails3 dev -port 9250` built, launched `bin/cairn.exe`, started Vite, and WebView2 connected; adding `zz_wails_hot_reload_probe.go` produced a second Wails `Build` marker and regenerated bindings; temporary `frontend/src/App.tsx` probe produced Vite `hmr update /src/App.tsx` and was served by the live dev server | Wails v3 pinned to `v3.0.0-alpha.99`; Go pinned to `toolchain go1.26.4`; logo/icon sourced from `assets/`. CI Linux runner changed to `ubuntu-24.04` in `dev-docs/08-packaging-release.md` because Wails alpha.99 defaults to GTK4/WebKitGTK 6.0. Golangci excludes frontend dependencies and build outputs. Browser plugin unavailable in this sandbox (`CreateProcessAsUserW failed: 5`), so hot-reload proof used Wails/Vite logs and live module fetch. |
| 0 | 0.2 SQLite store + migrations | green | `internal/store` with modernc SQLite, writer/reader handles, WAL/foreign-key/busy-timeout/synchronous pragmas; embedded `0001_v1_schema.sql` from `dev-docs/03-data-model.md`; forward-only migrator with newer-schema refusal and pre-migration backup retention; settings repository typed defaults/accessors; `go test ./internal/store`; `go test . ./internal/...`; `go vet . ./internal/...`; `golangci-lint v2.12.2 run --timeout=5m` | Aggregate-specific repos beyond settings will be added with their owning phases as their domain behavior lands. |
| 0 | 0.3 Event bus + error model + logging | green | `internal/bus` topic constants + in-process pub/sub with drop-oldest slow-subscriber policy and coalescing/batching helpers; `internal/apperror` contract error codes with wrap/unwrap helpers; `internal/logging` JSON structured logger with rotating file writer; `go test ./internal/bus ./internal/logging`; `go test` for `internal/apperror` via explicit test binary workaround; `go test` for all other repo packages; `go vet . ./internal/...`; `go build ./...`; `golangci-lint v2.12.2 run --timeout=5m`; frontend ESLint, Vitest, and `tsc --noEmit`; CI run 27433708316 green on Ubuntu 24.04, Windows, macOS | Local standard Vite production build under the bundled Codex Node runtime fails before app output with `EPERM` inside Vite/Rolldown while copying public assets/creating ignored `dist` files; manual Node/PowerShell writes to the same paths succeed. The normal CI frontend build is green. |
| 0 | 0.4 Wails services skeleton + bindings | green | `internal/models` DTO/enums from `dev-docs/04-api-contracts.md`; `internal/services` Wails-bound skeleton structs for all 12 API groups; shell registers services and marshals `AppError`; `wails3 generate bindings -clean=true -ts` produced 12 services, 115 methods, 8 enums, 80 models; frontend `api/` wrappers, `useEvent` hook, zustand skeleton stores, and `AppVersion()` render path; `go test . ./internal/...`; `go vet . ./internal/...`; `go build ./...`; `golangci-lint v2.12.2 run --timeout=5m`; frontend ESLint, Vitest, and `tsc --noEmit`; CI run 27434849787 green on Ubuntu 24.04, Windows, macOS | CI binding-diff guard added and green. Local standard Vite production build still has the bundled Codex Node `EPERM` issue documented in 0.3 notes; CI normal frontend build is green. |
| 0 | 0.5 Design system base | green | Core UI tokens/components added per `dev-docs/ui/00-design-system.md`; app shell placeholders use logo/icon assets from `assets/`; Ladle component catalog added and wired into CI; frontend ESLint, `tsc --noEmit`, Vitest, `npm audit --audit-level=moderate`; `go test . ./internal/...`; `go vet . ./internal/...`; `go build . ./internal/...`; `golangci-lint run --timeout=5m` with workspace cache; CI run 27438596807 green on Ubuntu 24.04, Windows, macOS | Local Ladle build compiles 1771 modules and reaches Rollup output, then the bundled Codex Node runtime hits `EPERM` writing ignored files under `frontend/dist/ladle`; direct Node/PowerShell writes to the same directory succeed. CI catalog build is green. |
| 0 | Exit gate 0 | green | `wails3 dev -config ./build/config.yml -port 9250` launched the Windows dev shell earlier in Phase 0; frontend calls backend `AppVersion()` through generated bindings; SQLite migration/settings tests pass; latest CI run 27438772984 green on Ubuntu 24.04, Windows, macOS with audit, binding diff, lint, Vitest, Ladle build, frontend build, Go tests, vet, and golangci-lint | Full Linux/macOS interactive dev-window launch and dedicated Windows `cairn-dev` WSL setup remain listed in `docs/manual-platform-validation.md` because those platform VMs/distros are unavailable in this environment. Per the goal rule for unavailable VMs, implementation continues with unit/CI validation and manual TODOs. |
| 1 | 1.1 Linux native provider (detect only) | green | `internal/providers` now has the provider interface, Linux-native Detect(), command runner, socket probe, manager active-provider selection, and provider-row persistence; `ProviderService` delegates to a manager when wired; fake-exec unit tests cover healthy, Docker missing, and `PERM_SOCKET`; store/manager tests cover status persistence and active selection; `wails3 generate bindings -clean=true -ts` clean; local `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint run --timeout=5m`, frontend ESLint/TypeScript/Vitest/audit green; CI run 27439750217 green on Ubuntu 24.04, Windows, macOS | Ubuntu CI exercised real Docker daemon detection. Local Windows host still has no `cairn-dev` WSL distro, so Windows WSL real-provider validation remains in manual VM TODOs. |
| 1 | 1.2 Docker client connect + ping + info | green | `internal/docker` wraps the official Docker Engine SDK (`github.com/docker/docker v28.5.2+incompatible`) with provider-derived host connection, API negotiation/minimum check, Ping/Info/Version/DiskUsage DTO mapping, `docker:connected`/`docker:disconnected` events, and reconnect backoff tests; `DockerService` delegates Ping/Info/Version/DiskUsage when wired; local `go test ./internal/docker` and `go vet . ./internal/...` green; CI run 27445592076 green on Ubuntu 24.04, Windows, macOS with audit, generated-binding diff, frontend lint/Vitest/Ladle/build, Go tests, Docker reconnect integration, vet, and golangci-lint | Ubuntu CI exercised real Docker daemon Ping/Info/Version/DiskUsage and stop/start reconnect, including socket-activated Docker by stopping `docker.socket` with `docker.service`. Local Windows host still has no `cairn-dev` WSL distro, so Windows WSL real-provider validation remains in manual VM TODOs. |
| 1 | 1.3 Object listing (containers/images/volumes/networks) | green | `internal/docker` has list/inspect/raw-inspect wrappers for containers/images/volumes/networks, DTO mapping, object cache writes, Docker event subscription with ≤250 ms coalesced `objects:changed`, and 60 s reconcile; `DockerService` delegates read-only object APIs; shell wires store/provider manager/Docker client/background loops/event forwarding; `wails3 generate bindings -clean=true -ts`; local `go test . ./internal/...`; `go vet . ./internal/...`; `go build . ./internal/...`; `golangci-lint v2.12.2 run --timeout=5m`; frontend ESLint, TypeScript, Vitest, and audit green; CI run 27446950969 green on Ubuntu 24.04, Windows, macOS | Ubuntu CI exercised the real-daemon object integration: scratch-built local image plus labeled container/volume/network, SDK-vs-CLI parity, external create/remove event within 1s, and cache reconcile. Local Windows host still has no `cairn-dev` WSL distro, so Windows WSL real-provider validation remains in manual VM TODOs. |
| 1 | 1.4 Read-only UI pages | green | Read-only inventory API/store snapshot; Overview, Containers, Images, Volumes, and Networks screens render live Docker data via generated bindings; tables include search/filter/sort, loading/empty/error states, status bar provider/engine state, and inspect JSON modal; frontend TypeScript, ESLint, Vitest, Vite dev build, and audit green; `go test . ./internal/...`; `go vet . ./internal/...`; `go build . ./internal/...`; `golangci-lint v2.12.2 run --timeout=5m`; `wails3 generate bindings -clean=true -ts`; CI run 27448017477 green on Ubuntu 24.04, Windows, macOS | E2E browser-level seeded-daemon coverage is represented locally by React integration tests until the dedicated E2E harness lands in later UI hardening. |
| 1 | 1.5 Container lifecycle actions | green | Docker SDK lifecycle wrappers for start/stop/restart/kill/remove with paused-container unpause path; `internal/security` risk mapper and 10-minute command-plan store; audit repository writes started/success/failed rows; DockerService safe actions, bulk start/stop/restart, kill/remove plan/apply path, and Settings audit read wiring; Containers UI row actions, bulk bar, and command-plan confirmation modal; generated bindings updated; local `go test . ./internal/...`; `go vet . ./internal/...`; `go build . ./internal/...`; `golangci-lint v2.12.2 run --timeout=5m`; frontend TypeScript, ESLint, Vitest, Vite dev build, and audit green; CI run 27448924362 green on Ubuntu 24.04, Windows, macOS | `dev-docs/04-api-contracts.md` updated because the prior contract had plan methods without an apply method for confirmed container plans. |
| 1 | 1.6 Run-image wizard + object creation parity | green | Backend Docker client/service support for RunImage, RenameContainer, PullImage, SaveImage, LoadImage, SearchHub, CreateVolume, and CreateNetwork; generated bindings; Images/Volumes/Networks/Containers UI modals and row actions; local `go test . ./internal/...`; `go vet . ./internal/...`; `go build . ./internal/...`; `golangci-lint v2.12.2 run --timeout=5m`; frontend TypeScript, ESLint, Vitest, Vite dev build, audit; in-app browser modal sanity check; CI run 27450211579 green on Ubuntu 24.04, Windows, macOS | Ubuntu CI exercised the real-daemon create/run/save/load integration: static scratch image, port-conflict validation, env/ports/volume/network inspect parity, rename, create volume/network, and save→load image ID preservation. Local Windows host still has no `cairn-dev` WSL distro, so Windows WSL real-provider validation remains in manual VM TODOs. |
| 1 | Exit gate 1 | green | CI run 27450211579 green; Phase 1.1–1.6 rows green with Docker read-only state, lifecycle controls, object creation, generated bindings, UI coverage, audit writes, and Linux real-daemon object integration | Cairn on Linux shows live Docker state, controls containers, can run/create objects, and auditing works. |
| 2 | 2.1 Compose CLI wrapper | green | `internal/compose` provider-backed CLI wrapper for `version`, `config`, `ps`, and `ls`; v2.2x parser fixtures; normative `testdata/projects` corpus; local `go test . ./internal/...`; `go vet . ./internal/...`; `go build . ./internal/...`; `golangci-lint v2.12.2 run --timeout=5m`; frontend TypeScript, ESLint, Vitest, Vite dev build, audit; CI run 27450933549 green on Ubuntu 24.04, Windows, macOS | Provider Compose execution now uses argv arrays with real workdir/env passthrough. Ubuntu CI exercises real `docker compose config` parsing across all committed sample projects. |
| 2 | 2.2 Project detector | green | `internal/compose.ProjectDetector` merges live Compose labels, `docker compose ls -a`, and imported rows; canonical `providerID/projectName` and `projectID/serviceName` IDs; project/service persistence repositories; stale detected-project pruning while preserving imported projects; ProjectService read/refresh and ComposeService config/ps wiring; local `go test . ./internal/...`; `go vet . ./internal/...`; `go build . ./internal/...`; `wails3 generate bindings -clean=true -ts`; `golangci-lint v2.12.2 run --timeout=5m`; frontend TypeScript, ESLint, Vitest, Vite dev build, audit; CI run 27451576159 green on Ubuntu 24.04, Windows, macOS | Label projects win over imported matches; imported workdir mismatches are flagged; missing imported workdirs are persisted with `E_WORKDIR_MISSING`; zero-container projects from `compose ls` are detected via resolved config. |
| 2 | 2.3 Projects page + project cards | green | Projects page with cards/list view, required filters/search/sort/view persistence, status/health/service/update/ports/workdir card states, and Import Project modal wired to backend validation/persistence; local `go test ./internal/services ./internal/shell`, `go test ./...`, `go vet ./...`, `go build ./...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest, Vite dev/prod build, audit, generated bindings, and in-app browser Projects/import modal check green; CI run 27452499124 green on Ubuntu 24.04, Windows, macOS | Import flow validates folders with `docker compose config`, persists imported project/service rows, returns `E_COMPOSE_INVALID` for missing/invalid folders, and previews compose-file candidates/project name in the UI. The app shell keeps the normative 1100×700 minimum window from `dev-docs/ui/01-app-shell.md`. |
| 2 | 2.4 Project lifecycle actions | green | Compose lifecycle wrappers for start/stop/restart/pull/up/down; `ProjectService` safe actions, `PlanRedeployProject`, `PlanDownProject`, and `ApplyProjectPlan` command-plan pipeline with typed-name confirmation for `down --volumes`; project audit rows and `job:progress`/`job:done` events; project card/list actions wired in UI; local `go test ./...`, `go vet ./...`, `go build ./...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest, Vite dev/prod build, audit, generated bindings, and in-app browser Projects/import modal sanity check green; CI run 27453069107 green on Ubuntu 24.04, Windows, macOS | `dev-docs/04-api-contracts.md` updated to add `ApplyProjectPlan(planID, typedName)` because planned project redeploy/down operations require an apply endpoint to satisfy the security pipeline. Workdir-missing lifecycle actions return `E_WORKDIR_MISSING` with a re-link hint. |
| 2 | 2.5 Project detail page (Overview/Services/Containers/Compose tabs) | green | Project detail backend now returns services, cached project containers, raw Compose files, and best-effort resolved `docker compose config` including non-fatal invalid-config errors; Projects UI opens detail with Overview, Services, Containers, and read-only Monaco Compose tabs plus validation/env-file badges; local `go test -cover ./...`, `go vet ./...`, `go build ./...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest, Vite dev build, generated bindings, and in-app browser Projects/import modal layout smoke green; CI run 27453681710 green on Ubuntu 24.04, Windows, macOS | Added build-multistage fixture coverage for resolved config/raw files/cached containers and invalid Compose validation path. Standalone Vite browser smoke cannot open real project detail without a Wails backend fixture, so detail tabs are covered by React integration tests plus service tests. Local non-coverage `go test ./...` still trips a Windows Defender false positive for `internal/services.test.exe`; coverage mode and CI normal tests pass. |
| 2 | Exit gate 2 | green | Phase 2.1-2.5 all green; latest CI run 27453771048 green on Ubuntu 24.04, Windows, macOS after the Phase 2.5 progress marker; Compose wrapper, detector, project import/listing, lifecycle command-plan pipeline, and project detail inspection are implemented with local unit/contract/frontend gates and browser smoke where available | MVP-2 Compose scope is complete: projects are detected, imported, controlled, and inspected. Local real Docker/WSL validation is still constrained by the unavailable `cairn-dev` distro noted in the Phase 0 manual-platform TODOs, while Linux real-daemon coverage is exercised in CI. |
| 3 | 3.1 Log streaming backend | green | `internal/logsvc` streams container/project/service/all logs with Docker stdout/stderr demux, timestamp parsing, level heuristics, per-stream ring buffer, <=50 ms/200-line batching, backpressure, cached pagination, `.log`/`.jsonl` export, dynamic project/service attachment, and Wails event forwarding; local `go test -cover ./internal/logsvc`, `go test -cover ./...`, `go vet ./...`, `go build ./...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint/Vitest/Vite build/audit; CI run 27454691050 green on Ubuntu 24.04, Windows, macOS including Linux real-Docker `big-logs` sustained 5,000 lines/s integration | Added the Linux-only CI log streaming integration. The hot log ring is a circular buffer so sustained streams keep O(1) append cost after the 50k-line retention window fills. |
| 3 | 3.2 Logs UI | green | Logs navigation/page added with scope picker for all/project/service/container streams, Wails `logs:lines`/`logs:eof`/`logs:error` event handling, virtualized 50k-buffer viewer, follow/unpin, pause buffering/resume banner, debounced search with match navigation and hide-nonmatching option, level/source filters, timestamp and wrap toggles, ANSI color rendering, skip-marker divider, and export modal/toast; frontend tests cover stream event rendering, search filtering, pause buffering, and export; local frontend ESLint, Vitest (25 tests), Vite dev build, audit, browser smoke at `http://127.0.0.1:5174` with no horizontal overflow, plus `go test -cover ./...`, `go vet ./...`, `go build ./...`, and `golangci-lint v2.12.2 run --timeout=5m`; CI run 27455226582 green on Ubuntu 24.04, Windows, macOS including Linux real-Docker log streaming integration | Standalone browser smoke runs in Wails browser-preview mode, so it verifies layout/empty/export modal states rather than live backend log events. The backend `ExportLogs` contract exports scope/path only, so UI export controls are constrained to the current stream scope. |
| 3 | 3.3 Stats collection + charts | green | `internal/metrics` collector with supervised Docker stats streams, adaptive visible/background sampling, CPU/rate math, in-memory latest samples, `stats:sample` stream, dashboard/top-N metrics, SQLite batch persistence, history queries, and retention/downsampling; local `go test ./internal/metrics` with 82.0% coverage, `go test -cover ./...`, `go test . ./internal/...`, `go vet ./...`, `go build ./...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend audit/ESLint/Vitest/Ladle/build, generated-binding check; CI run 27456287336 green on Ubuntu 24.04, Windows, macOS including Linux real-Docker metrics comparison against `docker stats` | Docker stats payloads can omit per-sample CPU counts on hosted Linux, so Cairn caches daemon `Info().CPUs` as the formula fallback. The real-Docker metrics integration uses a steady single-core container and non-stream sample for deterministic `docker stats --no-stream` tolerance, while production collection uses supervised streams with one-shot fallback after repeated stream errors. |
| 3 | 3.4 Charts UI + dashboard v1 | green | Dashboard v1 implements engine hero, counts strip, Recharts CPU/memory/network live area panel, project activity mini-list, status donut, recent containers, logs peek, updates card, top containers, recent events, prune confirmation preview, and project-card sparklines from `stats:sample`; frontend ESLint, Vitest (27 tests), Vite dev build, Ladle build, audit; in-app browser smoke at `http://127.0.0.1:5173` with no console errors and no horizontal overflow at 1280px or 390px; `go test -cover ./...`, `go vet ./...`, `go build ./...`, `golangci-lint v2.12.2 run --timeout=5m`; `wails3 generate bindings -clean=true -ts`; CI run 27457027576 green on Ubuntu 24.04, Windows, macOS | Wails native window minimum remains 1100x700 in `internal/shell/app.go`; CSS body min-width was removed so the dashboard can honor the `ui/03-dashboard.md` narrow stacked layout in browser/webview contexts. Prune UI enforces typed confirmation for volumes but only confirms the preview until a generic prune apply endpoint exists in the command-plan pipeline. |
| 3 | 3.5 Terminal backend + UI | green | `internal/terminal` manager with host/backend/project PTY sessions, Docker exec container sessions, shell detection, root detection, resize/write/close/list, terminal events, and curated 60+ command cheatsheet; Docker exec SDK wrapper + fake and real-daemon integration coverage; Terminal page with xterm tabs, container/project pickers, root badge, paste guard, cheatsheet copy/run behavior, and Ctrl/Cmd-K command palette safe-command path; generated Wails bindings; local `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (29 tests), Vite dev/prod build, Ladle build, audit; in-app browser Terminal smoke at `http://127.0.0.1:5173` with no horizontal overflow at 1280px or 390px; CI run 27458631983 green on Ubuntu 24.04, Windows, macOS including Linux real-Docker terminal exec integration | Added Linux CI terminal integration (`CAIRN_REAL_DOCKER_TERMINAL=1`) using scratch-built local images: one static `/bin/sh`/`/bin/test` shim for echo/exit-code round-trip and one shell-less image for `E_NOT_FOUND` fallback. Standalone Vite browser smoke runs in Wails browser-preview mode, so live terminal I/O is covered by Go unit/integration tests and frontend binding mocks. |
| 3 | Exit gate 3 | green | Phase 3.1-3.5 rows are locally and CI green; CI run 27459200636 green on Ubuntu 24.04, Windows, macOS; full Linux soak in dedicated `cairn-dev` WSL with `CAIRN_PHASE3_SOAK=1 CAIRN_PHASE3_SOAK_DURATION=4h go test ./internal/soak -run TestPhase3StreamsTerminalDashboardSoak -count=1 -timeout=4h15m -v` passed in 14402.259s with logs=288023, stats=14401, terminal_bytes=24470, dashboard_reads=480, baseline_goroutines=3, peak_goroutines=26, final_goroutines=8 | Phase 4 may start next. |
| 4 | 4.1 Linux permission options + degraded provider UX | green | `SettingsService.GetSettings/SetSetting` now round-trips validated SQLite settings including `linux.sudo_mode`; provider repair banner/dialog renders backend `ProviderProblem` repair hints and the three Linux permission choices (sudo per-action, docker group warning, rootless socket); Docker-stopped mode shows stale cached data, disables Docker mutations across inventory/project/image/volume/network/dashboard actions, and gates dashboard/log streams while degraded; local `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (31 tests), audit, App degraded/permission tests, and Vite dev build green; CI run 27465098900 green on Ubuntu 24.04, Windows, macOS | Phase 4.2 may start next. |
| 4 | 4.2 Error-state audit across UI | green | Backend notification repository/service wiring, header notification center with unread count/mark-all-read/deep-link states, and Ladle AppError matrix covering all 17 contract codes; local `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (33 tests), Vite dev build, Ladle build, and audit green; CI run 27465549532 green on Ubuntu 24.04, Windows, macOS | Phase 4.3 may start next. |
| 4 | 4.3 Perf pass at seed scale | green | Seed-scale UI/backend perf hardening implemented: large inventory tables now window rendered rows; container/image/volume/project filters avoid avoidable allocations and repeated count scans; frontend seed test covers 100 containers, 500 images, 200 volumes, 20 networks, and 10 projects with dashboard render <1.5s and filters <100ms; metrics seed test covers 100 stats streams, 500 images, 200 volumes, dashboard count/top queries, and goroutine/pprof leak review; env-gated Linux chaos harness covers daemon stop/start plus container churn. Local `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (36 tests), Vite dev build, Ladle build, audit, focused seed tests, WSL `cairn-dev` 30s chaos smoke, and required WSL `cairn-dev` 1h chaos run green (`churns=734`, `daemon_cycles=48`, `baseline_goroutines=3`, `peak_goroutines=45`, `final_goroutines=2`); CI run 27467785361 green on Ubuntu 24.04, Windows, macOS | Exit gate 4 may start next. |
| 4 | Exit gate 4 | green | Phase 4.1-4.3 rows are locally and CI green; Linux permission/degraded-provider UX is covered, error-state audit is complete, seed-scale performance targets are covered by frontend/backend tests, and the required WSL `cairn-dev` 1h chaos run passed with 734 churns, 48 daemon cycles, and goroutines returning below baseline. CI run 27467908950 green on Ubuntu 24.04, Windows, macOS. | Phase 5 may start next. |
| 5 | 5.1 WSL detection & exec layer | green | `WindowsWSLProvider` implemented with `wsl.exe --status` and UTF-16LE `wsl.exe -l -v` parsing, Docker Desktop distro exclusion, custom distro selection, WSL2/Ubuntu/systemd/Docker Desktop integration/Docker/Compose/Buildx/daemon checks, WSL command execution, host/backend shell commands, and Windows drive/UNC/backend path mapping. Parser/path/detection tests cover WSL missing, no Ubuntu distro, WSL1, systemd off, Docker Desktop integration conflict, configured `cairn-dev`, direct WSL exec, shell selection, and 23 path cases including spaces and unicode. Manual TODO updated with local `cairn-dev` evidence and remaining clean Win11 VM matrix cases. App default runtime wiring is deferred to 5.3 with the Docker API transport. Local `go test ./internal/providers`, `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (36 tests), and audit green; CI run 27468484069 green on Ubuntu 24.04, Windows, macOS. | Phase 5.2 may start next. |
| 5 | 5.2 Install flow inside Ubuntu | green | `WindowsWSLProvider.PlanInstall` emits a confirmed CommandPlan for WSL enablement, Ubuntu/custom-name install, WSL2 enforcement, systemd, Docker's official apt repo packages, docker group handling, WSL distro restart, service enable/start, and Docker/Compose/Buildx/hello-world verification. `ProviderService.ApplyInstall` streams `provider:install:progress` with `totalSteps` and records `provider.install` audit rows. Unit tests cover custom `cairn-dev` naming, step progress/execution, audit logging, plan expiry, and simulated apt/network failure repair hints. Local `go test ./internal/providers ./internal/services`, `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (36 tests), and audit green; non-destructive WSL verification in `cairn-dev` passed systemd, Engine 29.5.3, Compose 5.1.4, Buildx 0.34.1, and `hello-world`, with the image cleaned afterward. CI run 27469272819 green on Ubuntu 24.04, Windows, and macOS. | Clean Win11 VM installer and failure-injection cases are unavailable locally and tracked in `docs/manual-platform-validation.md`; Phase 5.3 may start next. |
| 5 | 5.3 Docker connection through WSL | green | Docker client accepts a provider-supplied `DockerDialContext`; Windows WSL exposes the Docker SDK through `wsl.exe -d <distro> -- docker system dial-stdio` with `socat UNIX-CONNECT:/var/run/docker.sock -` fallback, no TCP or npipe transport, and Windows app startup now wires `WindowsWSLProvider` by default. Unit tests cover SDK dialer injection, WSL detect host marker, dial-stdio command selection, socat fallback, and missing transport `E_PROVIDER_NOT_READY`; local `CAIRN_REAL_WSL_DOCKER=1 go test ./internal/docker -run TestWindowsWSLDockerConnection -count=1 -v` passed against `cairn-dev`; local `go test ./internal/providers ./internal/docker`, `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (37 tests), and audit green; CI run 27469876108 green on Ubuntu 24.04, Windows, and macOS. | Clean Win11 VM full Phase 1-3 integration rerun remains in `docs/manual-platform-validation.md`; Phase 5.4 may start next. |
| 5 | 5.4 Windows UX | green | Windows setup UX now has a WSL branch with welcome/backend/checks/install/verify steps, OS-aware backend cards, WSL performance-path guidance, provider check rows, CommandPlan preview, and `provider:install:progress` handling. Settings now includes a Providers-focused page with WSL distro setting, `provider.autostart_backend`, path-mapping info, and setup entry point; `providers.Manager` applies `windows.wsl_distro` before WSL detect/plan/start/stop/restart. Local `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (39 tests), Vite dev build, and audit green; CI run 27470600820 green on Ubuntu 24.04, Windows, and macOS. | Windows runner smoke is green; clean Win11 VM E2E smoke and fresh-machine checklist remain tracked in `docs/manual-platform-validation.md` as unavailable-platform manual TODOs. |
| 5 | Exit gate 5 | green | Phase 5.1-5.4 rows are locally and CI green; CI run 27470772442 green on Ubuntu 24.04, Windows, and macOS after the Phase 5.4 progress marker. Windows WSL provider detection, install planning/execution, SDK connection over WSL stdio, Windows setup onboarding, WSL settings, and path-performance guidance are implemented. Local Windows validation against the dedicated `cairn-dev` distro covered WSL2/systemd/Docker/Compose/Buildx/daemon checks, `hello-world`, and real Docker SDK Ping/Info/Version/ListContainers over WSL stdio. | Clean Win11 VM acceptance remains unavailable in this environment and is emitted as the manual checklist TODO in `docs/manual-platform-validation.md`; per the platform-VM exception, Phase 6 may start next. |
| 6 | 6.1 macOS Colima provider | green | `MacOSColimaProvider` implements Homebrew/Docker/Compose/Buildx/Colima/context/daemon detection, `colima status --json` and `docker context ls --format json` parsers, Homebrew-based install CommandPlan with progress execution, `colima start|stop|restart -p <profile>` lifecycle, profile CPU/RAM/disk settings, context-scoped Docker/Compose commands, identity path mapping, and zsh/`colima ssh` terminal commands. Darwin app startup and `NewDefaultManager` now include Colima by default. Local `go test ./internal/providers`, `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (39 tests), TypeScript, and audit green; CI run 27471354147 green on Ubuntu 24.04, Windows, and macOS. | Colima integration on a macOS runner/VM and Homebrew-present/absent manual rows are tracked in `docs/manual-platform-validation.md`; Phase 6.2 may start next. |
| 6 | 6.2 Existing-context provider | green | `ExistingContextProvider` implements `ctx:<name>` provider IDs, Docker context discovery via `docker context ls --format json`, context-scoped Docker/Compose commands with `--context`, SDK host resolution from context metadata, daemon ping, Compose/Buildx checks, unencrypted `tcp://` warning badge data, host shell fallback, and non-mutating active-context selection through `ProviderService.SetDockerContext`. `providers.Manager.ListDockerContexts` now returns real contexts and `SetDockerContext` creates/persists a dynamic existing-context provider without running `docker context use`. Local `go test ./internal/providers`, `go test . ./internal/...`, `go vet . ./internal/...`, `go build . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, frontend ESLint, Vitest (39 tests), TypeScript, and audit green; CI run 27471756787 green on Ubuntu 24.04, Windows, and macOS. | Real Docker Desktop/OrbStack/Rancher/remote context matrix is tracked in `docs/manual-platform-validation.md`; Phase 6.3 may start next. |
| 6 | 6.3 macOS onboarding branch + settings | green | macOS onboarding now branches to Colima with OS-aware backend cards, Colima profile/CPU/RAM/disk fields, provider check rows, install-plan preview, verify copy, and existing-context handoff into Settings. Settings now exposes macOS Colima resource/profile controls plus a Docker contexts table with refresh, current/available badges, unencrypted `tcp://` warning badge, and `ProviderService.SetDockerContext` activation without global Docker context mutation. React coverage exercises Colima checks/install planning and Colima settings/context switching. Local gates passed: frontend TypeScript, ESLint, Vitest (41 tests), Vite dev build, audit; `go test . ./internal/...`; `go vet . ./internal/...`; `go build . ./internal/...`; `golangci-lint v2.12.2 run --timeout=5m`. CI run 27472505884 green on Ubuntu 24.04, Windows, and macOS. | zsh host terminal behavior was implemented and tested in 6.1. Real macOS Colima onboarding, Colima-vs-Docker-Desktop context switching, and host/backend terminal smoke rows are tracked in `docs/manual-platform-validation.md` as unavailable-platform manual TODOs. |
| 6 | Exit gate 6 | green | Phase 6.1-6.3 rows are locally and CI green; CI run 27472645187 green on Ubuntu 24.04, Windows, and macOS after the Phase 6.3 progress marker. The macOS Colima provider, existing-context provider, macOS onboarding/settings UX, context-switching Settings path, Colima resource/profile controls, zsh host terminal, and `colima ssh` backend terminal command are implemented. | Real macOS Colima onboarding, Docker Desktop/OrbStack/remote context switching, and macOS terminal smoke remain unavailable in this environment and are emitted as manual checklist TODOs in `docs/manual-platform-validation.md`; per the platform-VM exception, Phase 7 may start next. |
| 7 | 7.1 Backup/restore engine | green | `internal/backups` manager implements plan/apply/list/delete for volume backups and restores through provider Docker helper containers, checksum sidecars, SQLite backup records, audit rows, job events, running-container warnings, free-space checks, typed-name overwrite confirmation, and provider-stable plan execution. Local `go test ./internal/backups`; `go test . ./internal/...`; `go vet . ./internal/...`; `go build . ./internal/...`; `golangci-lint v2.12.2 run --timeout=5m`; real Windows-hosted WSL validation passed with `CAIRN_REAL_WSL_DOCKER_BACKUPS=1 go test -tags=wslintegration ./internal/backups -run TestManagerRealWSLDockerBackupRestoreRoundTrip -count=1 -timeout=3m -v`; CI run 27475067191 green on Ubuntu 24.04, Windows, and macOS, including Linux `CAIRN_REAL_DOCKER_BACKUPS=1`. | Local `.tools/wails3.exe` is currently wedged even for `version`; generated bindings were already regenerated after the API change and CI binding generation/diff is green. |
| 7 | 7.2 Minimal v1 backup UI | green | Volumes page row actions now open Backup/Restore modals; project detail has a Backups tab with project-volume backup actions and restore rows; confirmation modal routes backup/restore plans through `BackupService.ApplyBackup` and typed-name `ApplyRestore`; frontend TypeScript, ESLint, Vitest (42 tests), Vite dev build, and audit passed; CI run 27475067191 green. | Full backup browser remains P2 per plan. |
| 7 | Exit gate 7 | green | CI run 27475067191 green on Ubuntu 24.04, Windows, and macOS; Linux CI and local `cairn-dev` WSL both verified backup and restore with real named-volume data; restore overwrite requires typed target name; backup/restore are callable as command-plan apply steps. | Phase 8 may start next. |
| 8 | 8.1 Registry client + accounts | green | `internal/registry` package added with ref normalization corpus, backend Docker config account parsing, credential-helper list/get support, provider-exec `docker login --password-stdin`, logout/TestAuth, Bearer token auth, HEAD manifest digest resolution, multi-arch index selection, cache/backoff/concurrency scaffolding; `RegistryService` and app shell wired; provider stdin/backend command support added; Linux CI registry auth integration step added. Local `go build . ./internal/...`, `go vet . ./internal/...`, `golangci-lint v2.12.2 run --timeout=5m`, `go test -c` for registry/providers/services, and Linux cross-compile of `internal/registry` tests green. CI run 27476475911 green on Ubuntu 24.04, Windows, and macOS, including Linux `CAIRN_REAL_DOCKER_REGISTRY=1` auth-protected registry login/push/digest/logout integration. | Local Windows `go test` execution remains unreliable because of a stuck/unreapable `go.exe` phantom process, but CI normal unit tests and the Linux real-registry integration are green. |
| 8 | 8.1a Image tag & push | green | Backend Docker SDK `TagImage`/`PushImage` implemented with distribution/reference validation, Docker credential-chain auth encoding, `image:push:progress` events, registry-auth/rate-limit error mapping, object-change events, and `image.tag`/`image.push` audit rows. Images UI has Tag and Push modals, exact `docker push <ref>` preview, confirmation checkbox, auth-chip/login affordance, per-layer progress, and copyable pull command; Settings has Registries account table/login/test/logout with plaintext config warning. Local checks green: frontend `npm run build:dev`, `npm test` (42 tests), `npm run lint`; Go `go build . ./internal/...`, `go vet . ./internal/...`, `golangci-lint run --timeout=5m`, and compile-only `go test -c` for docker/registry/services. CI run 27477495540 green on Ubuntu 24.04, Windows, and macOS; Ubuntu included `CAIRN_REAL_DOCKER_REGISTRY=1` registry auth integration and registry tag/push integration. | Local Windows `go test` execution still hangs behind the old phantom Go runner, but CI normal Go unit tests and the Linux real-registry tag→push→pull digest round-trip are green. Push confirmation uses the existing generated `PushImage(ref)` API with exact-ref UI confirmation and backend `needs_confirmation` audit because the current contract surface has no generated image push plan/apply endpoint and local Wails binding generation remains wedged. |
| 8 | 8.2 Dockerfile parser + lineage discovery | green | `internal/lineage` now has the hand-rolled Dockerfile `FROM` parser (32 fixtures covering multi-stage, ARG substitution, platform flags, comments, continuations, scratch, digest pins, and stage refs), lineage discovery from persisted Compose build config + Dockerfile + OCI/Cairn labels, confidence assignment, SQLite persistence for `image_lineage`/`base_image_refs`, and Cairn build-label helpers plus a `docker compose build` wrapper that emits sorted `--label io.cairn.*` args. Golden tests read `testdata/projects/{build-simple,build-multistage,mixed-updates}/expected.json`; `mixed-updates` now includes lineage expectations for build services. Local checks green: focused parser/label tests with coverage, focused Compose build-label test, lineage manager golden/confidence/get/refresh/missing-project tests, store lineage replace/lookup test, `go build . ./internal/...`, `go vet . ./internal/...`, `golangci-lint run --timeout=5m`, and compile-only test binaries for compose/lineage/store/services/shell. CI run 27479198573 green on Ubuntu 24.04, Windows, and macOS after fixing the initial SQLite deadlock in lineage list hydration. | Broad local multi-package `go test` still sometimes hangs behind the existing phantom Go/test processes, so full normal suite validation is via CI. |
| 8 | 8.3 Update checker | green | `internal/updates` checker implemented with service-image digest comparison, base-image digest comparison through persisted lineage, status mapping for update/up-to-date/pinned/built/local-only/auth/rate-limit/error/unknown-base, manual `CheckAllUpdates` progress events, scheduler scaffolding from `updates.check_interval_hours`, ignore/unignore overlay, current-update/history reads, SQLite persistence via `store.UpdateRepository`, lineage `base_image_refs` check hydration, and project `UpdateBadges` hydration. Normative cases 1-8 and 11 are covered with mock registry/image tests; `internal/updates` focused tests pass with 80.3% statement coverage. Local checks green: `go test ./internal/updates -count=1 -cover -timeout=90s`, focused store ignore/badge test, `go build . ./internal/...`, `go vet . ./internal/...`, `golangci-lint run --timeout=5m`, and compile-only test binaries for updates/store/services/shell. CI run 27480667911 green on Ubuntu 24.04, Windows, and macOS after restoring Linux CI to use the runner's preinstalled Docker Engine/Compose/Buildx. | Broad local multi-package `go test` still sometimes hangs behind the existing phantom Go/test processes, so the full normal suite validation is via CI. Phase 8.4 may start next. |
| 8 | 8.4 Update planner + executor + health watch + rollback | green | Update planner/executor backend implemented: service/project plans include ordered mixed pull/build/up commands, non-actionable warnings, 10-minute plan expiry, backup-first execution via Phase 7 backup engine, rollback snapshots in `update_history`, audited apply/rollback jobs with `job:progress`/`job:done`, 60s health-watch semantics with healthcheck/restart/log heuristics, auto-rollback when old image is local, and manual-needed history when old image is pruned. Local checks green: `go test ./internal/updates -count=1 -cover -timeout=120s` at 81.3% statement coverage, compile-only test binaries for store/compose/backups/services/shell, `go build . ./internal/...`, `go vet . ./internal/...`, and `golangci-lint run --timeout=5m`. CI run 27481515874 green on Ubuntu 24.04, Windows, and macOS; Ubuntu covered the real-Docker reconnect, logs, metrics, terminal, backup, registry auth, and registry tag/push integrations. | Local Windows package tests outside `internal/updates` still sometimes hang behind the existing phantom Go/test processes, so compile-only plus CI are used for those packages. Phase 8.5 may start after the marker CI is green. |
| 8 | 8.5 Updates UI + lineage UI | green | Updates page added with Current/History/Ignored tabs, filter chips, check-now progress, grouped project rows, per-service/project update planning, confirmation/progress modal with commands/digests/options, history rollback, ignored unignore flow, project Updates tab grouping, and container inspect Image Lineage card with the normative unknown-base copy. Project cards keep split update badges, and update notifications now deep-link to the Updates page. Frontend mocks and tests cover the global Updates journey, modal apply options, history rollback, ignored unignore, project Updates tab grouping, lineage confidence wording, unknown-base wording, and update notification routing. Local checks green: frontend `npm run audit`, `npm run lint`, `npm test` (44 tests), `npm run ladle:build`, `npm run build:dev`, `npm run build`; Go `go test ./internal/metrics -count=1`, `go build . ./internal/...`, `go vet . ./internal/...`, and `golangci-lint run --timeout=5m`. CI run 27482377039 green on Ubuntu 24.04, Windows, and macOS; Ubuntu covered the real-Docker reconnect, logs, metrics, terminal, backup, registry auth, and registry tag/push integrations. | A brittle metrics integration memory comparison was stabilized with an absolute+relative tolerance for Docker accounting noise while preserving parser coverage. Local Windows package tests outside focused packages can still hang behind existing phantom Go/test processes, so full-suite validation is via CI. |
| 8 | Exit gate 8 | green | Phase 8.1-8.5 rows are green, and marker CI run 27482496569 passed on Ubuntu 24.04, Windows, and macOS. The 14 normative registry/update/lineage cases are covered across registry, lineage, updates, Docker, store, and frontend tests: digest/status mapping, mutable-tag warning, pinned/local/auth/rate-limit/error states, base-image rebuild detection, multi-stage and ARG lineage, unknown-base copy, mixed pull/build/up ordering, health failure rollback/manual guidance, ignore/unignore badges, registry login/logout/auth, tag/push/pull digest round-trip, and plaintext Docker config storage labeling. Linux CI exercised real Docker registry auth and tag/push, and the update/rebuild/tag-push code paths are implemented through provider-scoped Docker/Compose execution for all providers. | Clean Windows WSL and macOS Colima runtime smoke for one-click update, rebuild, and registry tag/push remain emitted as manual TODOs in `docs/manual-platform-validation.md` because those clean platform VMs are unavailable here. Phase 9 may start next. |
| 9 | 9.1 Settings page complete | green | Settings now has the section rail from `dev-docs/ui/10-settings.md`, all normative settings keys from `dev-docs/03-data-model.md §7` wired through `SettingsService.SetSetting`, provider-specific WSL/Linux/Colima groups, Docker contexts, registry credential mode, theme controls, update interval, save toasts, and about/version details. Local checks green: frontend `npm run lint`, focused `npm test -- --run src/App.test.tsx`, full `npm test` (45 tests), `npm run build`, `go test ./internal/services -count=1`, and `go test ./internal/store -count=1`. CI run 27483134638 passed on Ubuntu 24.04, Windows, and macOS. | Audit viewer/update notice remain Phase 9.2, and cheatsheet polish remains Phase 9.3. |
| 9 | 9.2 Audit log viewer, filters, notifications, update notice | green | Security & Audit now has an audit table with range/action/status/project filters, row details, CSV export, and audit metadata for target type/duration; notification center retains unread count/list/deep links/mark-all-read and now includes app-update notices; app release checks query GitHub releases/latest after settings load and respect `updates.notify`. Local checks green: frontend `npm run lint`, focused `npm test -- --run src/App.test.tsx` (37 tests), full `npm test` (47 tests), `npm run build`, `go test ./internal/store -count=1`, `go test ./internal/services -run TestProviderServiceApplyInstallPublishesProgress -count=1`, `go test ./internal/services -run TestDockerServiceLifecycleAuditsAndPlans -count=1`, and `go test ./internal/store -run TestAuditListIncludesViewerMetadata -count=1`. CI run 27483933925 passed on Ubuntu 24.04, Windows, and macOS; follow-up stabilization CI run 27484254650 also passed on Ubuntu 24.04, Windows, and macOS. | The local Wails CLI remains wedged even for `version`, so no generated-binding contract expansion was made; 9.2 stays within the existing generated `AuditFilter{topic,limit}` contract and applies secondary filters client-side. Broad local Go package runs remain affected by stale Windows Go test processes, so the full package sweep is via CI. |
| 9 | 9.3 Cheatsheet finalized | green | Cheatsheet content now has a reviewed v1 command set across the normative Terminal categories (containers, images, compose, volumes, networks, logs, exec, stats/debug, cleanup), with >=60 entries, explicit placeholders, no duplicate commands, and runnable rows restricted to `safe`. Risk labels were aligned to `dev-docs/05-security.md §2` for container lifecycle, image pull/tag/save/load/push/remove, Compose update/rebuild/down, volume/network create/remove/prune, and Docker cleanup commands. The command palette now has frontend coverage proving dangerous commands copy only and do not open/write terminal sessions. Local checks green: focused `go test ./internal/terminal -run TestCheatsheet -count=1`, `go test ./internal/services -run TestSettingsServiceGetCheatsheetSafetyContract -count=1`, focused `npm test -- --run src/App.test.tsx`, frontend `npm run lint`, full `npm test` (48 tests), and `npm run build` with generated `frontend/dist` assets cleaned afterward. CI run 27484689469 passed on Ubuntu 24.04, Windows, and macOS, including Ubuntu real-Docker integrations. | Local `go test ./internal/terminal -count=1` timed out on this Windows host alongside the existing stale `go test ./...` process table entry; full package validation is covered by CI. |
| 9 | Exit gate 9 | green | Phase 9.1-9.3 rows are green, and every sidebar page is functionally complete for v1 scope: Dashboard, Projects, Containers, Images, Volumes, Networks, Logs, Terminal, Updates, Settings, Audit/Security, notifications, command palette, and cheatsheet all have implemented loading/empty/error or degraded states, command-plan/confirmation flows where required, and tests across frontend/backend/CI. Phase 9 marker CI run 27484810301 passed on Ubuntu 24.04, Windows, and macOS; Ubuntu covered the real-Docker reconnect, logs, metrics, terminal, backup, registry auth, and registry tag/push integrations. | Phase 10 polish/release remains: final onboarding sweep, global empty/error/loading/a11y review, packaging/signing, full release matrix, 24 h soak, docs, and release notes. Clean Windows WSL and macOS Colima runtime smoke remain manual TODOs in `docs/manual-platform-validation.md` because those clean platform VMs are unavailable here. |
| 10 | 10.1 Onboarding final pass | green | Linux-native install planning/execution is implemented with the existing command-plan/progress pipeline; onboarding now uses the complete Welcome -> Backend -> Checks -> Install -> Verify -> Projects -> Done flow with OS-aware Windows WSL, Linux native, macOS Colima, and existing-context branches; setup state persists for resume-after-restart; detected projects can be selected, skipped, or imported manually; Linux socket permission choices persist through the same `linux.sudo_mode` setting as provider repair. Local frontend gates are green: focused `npm test -- --run src/App.test.tsx` (42 tests), full `npm test` (52 tests), `npm run lint`, and `npm run build` with generated `frontend/dist` assets cleaned afterward. The new Linux provider tests passed once via focused `go test ./internal/providers -run TestLinuxNative -count=1` before this Windows host's local Go runner wedged again; subsequent provider package commands timed out before emitting test output and fresh `go`/`providers.test` processes were cleaned, leaving only the pre-existing phantom PID 32620. CI run 27485618177 passed on Ubuntu 24.04, Windows, and macOS, including Ubuntu real-Docker integrations. | Phase 10.2 empty/error/loading/a11y review may start next. |
| 10 | 10.2 Empty/error/loading/a11y sweep | green | App shell keyboard flow now focuses global search with `/` outside editable fields while preserving Ctrl/Cmd-K command palette behavior. Data tables now expose per-table aria labels, column counts, named selection cells, row-label-aware checkboxes, and explicit sort button labels for unsorted/ascending/descending states. React coverage exercises the `/` search shortcut and the improved table labels/sort/selection semantics. Local frontend gates are green: full `npm test` (52 tests), `npm run lint`, and `npm run build` with generated `frontend/dist` assets cleaned afterward and the embed placeholder restored. CI run 27486030871 passed on Ubuntu 24.04, Windows, and macOS; Ubuntu covered the real-Docker reconnect, logs, metrics, terminal, backup, registry auth, and registry tag/push integrations. | Phase 10.3 packaging/signing prep may start next. |
| 10 | 10.3 Packaging/signing prep | green | Packaging metadata has been corrected for the v1 targets: Windows NSIS defaults to per-user install, Linux default packaging is AppImage + deb only, macOS packaging now emits a dmg, package metadata uses Cairn names/copyright/homepage, and uninstall/install scripts refresh Linux desktop/icon caches without touching Docker groups. Release helper scripts sync source artwork from `assets/`, generate Linux icon sizes in CI, stamp versions from `vX.Y.Z` into Wails metadata and backend `VersionInfo` ldflags, and validate produced artifacts. Frontend has a stamped-version fallback for backend version-load failure. Local checks are green: asset sync smoke, version-stamp smoke, workflow YAML parse, frontend `npm test` (53 tests), `npm run lint`, `npm run build` with generated `frontend/dist` assets cleaned afterward, temp-root artifact checker validation for Windows/Linux/macOS, and `git diff --check`. CI run 27487171104 passed on Ubuntu 24.04, Windows, and macOS, including package-smoke builds and artifact checks for NSIS, AppImage, deb, dmg, and the macOS app bundle. | Local Wails/Go packaging cannot be trusted on this Windows host because the known phantom Go runner still wedges local `go run`/Wails invocations. Phase 10.4 release matrix dry run may start after the marker CI is green. |
| 10 | 10.4 Full matrix, soak, perf, security review | green | Current full-matrix CI run 27502520080 is green for commit `0c2a928` across Windows/macOS/Ubuntu lint-unit and package-smoke jobs, including NSIS/AppImage/deb/dmg checks, Ubuntu `.deb` install/uninstall, Debian stable container `.deb` install/uninstall, release DB upgrade fixture, expanded security suite, Ubuntu real-Docker integrations, Ubuntu release validation, and seeded release UI performance. Local 2026-06-15 release validation is green: `./scripts/run-release-validation.ps1 -Suite checklist,manual-matrix,soak-checker,upgrade-fixtures,security,performance,ui-release,wsl-provider` covered release ledgers, soak checker, DB upgrade, security, seed-scale performance, 16-check release UI, and WSL SDK/backup/registry/update smokes. The real 24 h WSL/Linux soak `phase10-24h-20260614T071038Z` completed and `scripts/check-soak-status.ps1 -RequireComplete` validated duration `24h0m0s`, logs `1728027`, stats `86401`, terminal bytes `146870`, dashboard reads `2881`, baseline goroutines `3`, peak `35`, final `8`, exit code `0`. Windows `bin/cairn.exe` and `bin/cairn-amd64-installer.exe` were rebuilt locally; `scripts/check-release-artifacts.ps1 -Platform windows` passed. | Environment-available automated release evidence is complete. Clean Windows/macOS/Linux desktop installed-app matrix rows, Debian desktop/AppImage/rootless rows, third-party context hardware/VM rows, and production signing/notarization credentials remain external release sign-off items tracked in `docs/manual-platform-validation.md` and `docs/v1-release-checklist.md`. |
| 10 | 10.5 Docs, troubleshooting, release notes | green | Added `docs/user-quickstart.md`, `docs/provider-troubleshooting.md`, and `docs/release-notes-v1.0.0.md`. Updated `docs/v1-release-checklist.md` and `docs/v1-release-validation.md` with completed soak, current release validation, visual baseline, Windows package, and remaining external sign-off evidence. | Public website/update publication remains post-tag release process work per `dev-docs/08-packaging-release.md`. |
| 10 | Exit gate 10 | blocked | Cairn v1.0 implementation, docs, local Windows WSL validation, 24 h soak, Windows exe/installer build, and local/CI automated release evidence are complete in this workspace. | Public v1.0 release sign-off is blocked only by external clean-machine/manual installed-app validation and signing/notarization credentials that are unavailable in this environment. |

## Post-Review Fixes

| Date | Scope | Status | Evidence | Notes |
|---|---|---:|---|---|
| 2026-06-15 | Runtime provider switching, provider restart confirmation, Docker object prune/remove execution, stale update/dashboard controls | green | `wails3 generate bindings -clean=true -ts`; `npm test` (53 tests); `npm run lint`; `npm run build`; `npx tsc --noEmit`; `go test . ./internal/...`; `git diff --check`; `.tools/wails3.exe task windows:build` refreshed `bin/cairn.exe` | `frontend/dist` was restored to placeholder-only after builds. `npm run format:check` remains a baseline repo-wide failure across existing/generated frontend files, so no mass formatting was applied. |
| 2026-06-15 | Claude review Sprint 1 safety/correctness fixes | green | `go test ./internal/store ./internal/services ./internal/metrics ./internal/shell`; `go test -race ./internal/services ./internal/shell`; `go test . ./internal/...`; `npx tsc --noEmit`; `npm test` (53 tests); `npm run lint`; `npm run build`; `.tools/wails3.exe task windows:build` with `.tools` on `PATH` | Fixed runtime service rebind field races with a shared RW guard, provider install cancellation, separate cached container `state`, deterministic volume/network command previews, IPv6 publish preview formatting, private SQLite store directory creation, bounded metrics pending queue, and Wails runtime bundling. `@wailsio/runtime@3.0.0-alpha.99` is not published on npm, so Vite externalizes `@wailsio/runtime` to Wails' bundled `/wails/runtime.js` while keeping npm alpha.79 as the type source. C3/C4 typed-name enforcement was not broadened because `dev-docs/05-security.md` requires typed-name for `dangerous`, while `destructive` uses modal/effects confirmation; changing that needs an explicit spec update. `frontend/dist` restored to placeholder-only after build. |
| 2026-06-15 | Claude review Sprint 2 resource lifecycle fixes | green | `go test ./internal/docker ./internal/backups ./internal/updates ./internal/shell`; `go test -race ./internal/docker ./internal/backups ./internal/updates ./internal/shell`; `go test . ./internal/...`; `go build . ./internal/...`; `go vet . ./internal/...`; `git diff --check` | Serialized Docker connect/disconnect/close to prevent reconnect client leaks, added runtime-owned cancellable jobs for backup/restore/update/check/rollback work, wired backup/update managers into provider runtime shutdown, and hardened backup filename allocation against non-missing stat errors plus unbounded collision loops. |
| 2026-06-15 | Claude review Sprint 3 backend robustness fixes | green | `go test ./internal/logsvc ./internal/terminal ./internal/store ./internal/backups ./internal/updates`; `go test -race ./internal/logsvc ./internal/terminal ./internal/store ./internal/backups ./internal/updates`; `go test . ./internal/...`; `go build . ./internal/...`; `go vet . ./internal/...`; `git diff --check` | Made log stream enqueue bounded with skipped-line accounting, detached terminal close/exit-code inspection from request cancellation, added SIGTERM-before-SIGKILL for Unix PTYs, made store migration backups publish atomically via temp+rename, made restore helper extraction depend on successful target wipe, narrowed update fatal-log matching, and switched default update jitter to OS randomness. |
| 2026-06-15 | Claude review Sprint 4 frontend event de-dup + first App split | green | `npm test -- --run src/App.test.tsx`; `npx tsc --noEmit`; `npm run lint`; `npm test`; `npm run build`; `git diff --check` | Centralized `objects:changed`/`provider:changed` refresh handling through `useDebouncedRuntimeEvent`, removed the Overview-owned duplicate object subscription, added a regression assertion for exactly one `objects:changed` listener, and moved the dashboard top-row helper into `frontend/src/overview/dashboardData.ts`. `frontend/dist` restored to placeholder-only after build. |
| 2026-06-15 | Claude review Sprint 5 theme-aware dashboard charts | green | `npm test -- --run src/overview/dashboardData.test.ts` (5 tests); `npx tsc --noEmit`; `npm run lint`; `npm test` (58 tests); `npm run build`; `git diff --check` | Dashboard/Recharts/Sparkline colors now use CSS theme variables through `chartColors`, container-status pie segments are generated by the overview helper, and focused tests lock the CSS-variable contract plus dashboard top-row/status-segment behavior. `frontend/dist` restored to placeholder-only after build. |
| 2026-06-15 | Windows backend terminal ConPTY fix | green | `go test ./internal/terminal ./internal/providers -run "TestWindowsPTYStarter|WindowsWSL" -count=1`; `go test -race ./internal/terminal -count=1`; `go test ./internal/... -count=1`; `npm run build`; `.tools/wails3.exe task windows:build APP_VERSION=1.0.0` | Replaced the Windows PTY stub with ConPTY-backed terminal sessions, added a Windows smoke test that reads real terminal output, and launched WSL backend/project terminals with `wsl.exe --cd <mapped backend path>` so selected distro/project working dirs are honored. Rebuilt `bin/cairn.exe`; `frontend/dist` restored to placeholder-only after build. |
| 2026-06-15 | Windows WSL project path normalization | green | `go test ./internal/compose ./internal/services ./internal/shell -run "Path|Detector|LifecycleMaps|ClientMaps|Runtime" -count=1`; `go test ./internal/... -count=1`; `go test -race ./internal/compose ./internal/services -count=1`; `npm run build`; `.tools/wails3.exe task windows:build APP_VERSION=1.0.0` | Compose detection now maps backend label paths such as `/mnt/e/...` to host paths for local stat/read/UI behavior, while Compose execution maps host project files back to backend paths before invoking the provider. This prevents healthy WSL projects from being marked `error`/`Workdir missing` and keeps lifecycle actions working for cached backend-path records. |
| 2026-06-15 | Windows-cleaned WSL path mapper fix | green | `go test ./internal/providers -run WindowsWSL -count=1`; `go test ./internal/compose ./internal/services ./internal/providers -run "WindowsWSL|Path|Detector|LifecycleMaps|ClientMaps" -count=1`; `go test ./internal/... -count=1` | Windows WSL path mapping now accepts WSL absolute paths that Go/Windows cleaned into backslash form, such as `\mnt\e\...`, and normalizes them back to `/mnt/e/...` before backend Compose execution or host path mapping. |
| 2026-06-15 | Windows WSL capability-first distro detection | green | `go test ./internal/providers -run WindowsWSL -count=1`; `git diff --check`; `npm run build`; `windres --target=pe-x86-64 ...`; direct production `go build ... -o bin/cairn.exe`; broad `go test ./internal/... -count=1` emitted package `ok` lines before Windows refused to unlink a temp `docker.test.exe`; isolated temp retry wedged in the known local Go runner state | Runtime detection no longer rejects a selected WSL distro solely because `/etc/os-release` is not Ubuntu-family. Cairn now probes systemd, Docker CLI, Compose, Buildx, and daemon reachability first; Ubuntu-family guidance is retained only in repair hints for the automatic installer path. Wails CLI is currently wedged locally even for `version`, so this exe was rebuilt with the same production Go flags plus `windres`-compiled Cairn icon/manifest resources. |

## Current Manual Platform TODOs

Local Windows WSL validation is now available through the dedicated `cairn-dev` distro and has covered Phase 0.0, Phase 5.1 detection/path behavior, the non-destructive final verification portion of Phase 5.2, Phase 5.3 Docker SDK connection over WSL stdio, Phase 5.4 Windows UX behavior through unit/frontend/CI coverage, Phase 7 real backup/restore round-trip, and Phase 8 registry tag/push plus update/rebuild round-trips through the Windows WSL provider. `./scripts/run-release-validation.ps1 -Suite wsl-provider` now runs those real WSL smokes together and checks that Windows Docker contexts remain unchanged. Clean Win11 VM installer/failure-injection/full-suite cases remain tracked in `docs/manual-platform-validation.md`; macOS Colima VM validation for Phase 6.1, real third-party Docker context validation for Phase 6.2, and Phase 8 update/rebuild/tag-push runtime smoke on macOS Colima are tracked there as unavailable in this environment.
